#include <stdio.h>
int main()
{
  int p, r ,t;
  float si, a;
  
  scanf("%d", &p);
  scanf("%d", &r);
  scanf("%d", &t);
  
  si= (p*r*t)/100;
  a=si+p;
  
  printf("Interest=%.2f\n" , si);
  printf("Amount=%.2f" , a);

	return 0;
}