#include <stdio.h>
#include<math.h>
float a,b;
int main()
{
	scanf("Enter a:");
 	scanf("Enter b:");
  	scanf("%f",&a);
  	scanf("%f",&b);
  printf("sum=");
  printf("%0.0f",floor(a)+floor(b));
  
	return 0;
}