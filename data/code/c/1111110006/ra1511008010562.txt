#include <stdio.h>
#include<math.h>
int main()
{
  float num1;
  scanf("%f",&num1);
  float num2;
  scanf("%f",&num2);
  int sum;
  sum=floor(num1)+floor(num2);
  printf("sum=%d",sum);
  

	return 0;
}