#include <stdio.h>
int main()
{
	int n,pin;
  	char place[30];
  scanf("%d",&n);
  scanf("%s",place);
  scanf("%d",&pin);
  printf("Address");
  printf("\nHouse Number:%d",n);
  printf("\nArea:%s",place);
  printf("\nPin Code:%d",pin);
  return 0;
}