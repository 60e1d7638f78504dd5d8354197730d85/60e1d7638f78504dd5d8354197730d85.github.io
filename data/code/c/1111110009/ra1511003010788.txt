#include <stdio.h>
int main()
{
	char n[20];
  	int a;
  	char p[15];
  	char d[20];
  	scanf("%s %d %s %s", n, &a, p, d);
  	printf("Name:%s\n",n);
  	printf("Age:%d\n",a);
  	printf("Place:%s\n",p);
  	printf("Department:%s",d);
	return 0;
}
