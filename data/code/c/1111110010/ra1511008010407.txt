#include <stdio.h>
int main()
{
  int arr[10], i=0, j=0, k=0;
  for(i=0; i<10; i++)
    scanf("%d", &arr[i]);
  for(j=0; j<10; j++)
  {
    printf("%d", arr[j]);
    k++;
    if(k%3==0)
      printf("\n");
    else if(j!=9)
      printf(",");
  }

	return 0;
}