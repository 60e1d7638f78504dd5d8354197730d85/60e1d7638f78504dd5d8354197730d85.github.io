#include <stdio.h>
int main()
{
 float age;
    //printf("entert he people age");
         scanf("%f",&age);
         if(age>=18 && age<=60)
         {
         printf("Eligible  ");
         }      else
                {printf("Not Eligible");
                }
	return 0;
}