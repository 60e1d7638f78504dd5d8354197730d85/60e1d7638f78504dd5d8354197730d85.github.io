#include <stdio.h>
#include<math.h>
int main()
{
  float a,b,c,p,area,s;
  scanf("%f%f%f",&a,&b,&c);
  p=a+b+c;
  s=p/2;
  area=sqrt(s*(s-a)*(s-b)*(s-c));
  printf("perimeter of triangle is=%f", area);
  return 0;
}