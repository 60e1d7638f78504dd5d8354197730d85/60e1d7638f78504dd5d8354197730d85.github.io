#include <stdio.h>
int main()
{
int num, digit_At_ones_placce; 
scanf("%d",&num); 
digit_At_ones_placce=num%10; 
printf("\nThe Digit at ones place of %d is=%d", num,digit_At_ones_placce); 
	return 0;
}