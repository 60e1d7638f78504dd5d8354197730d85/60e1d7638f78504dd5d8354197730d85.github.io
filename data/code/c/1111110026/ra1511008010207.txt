#include <stdio.h>
int main()
{
   int x,y,temp=0;
   scanf("%d%d", &x,&y);
   printf("\nBefore swapping:%d %d", x,y);
    temp = x;
     x  = y;
    y = temp;
   printf("\nAfter swapping:%d %d", x,y);
    
	return 0;
}