#include <stdio.h>
int main()
{
	float a,b,c,d,sum;
  	scanf("%f",&a);
  	scanf("%f",&b);
  	scanf("%f",&c);
  	scanf("%f",&d);
  	sum=(10*a)+(5*b)+(2*c)+(1*d);
  	printf("Total amount in the piggybank=%.2f",sum);
	return 0;
}