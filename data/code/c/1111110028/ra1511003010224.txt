#include <stdio.h>
int main()
{float sum=0,a,b,c,d;
 scanf("%f",&a);
 scanf("%f",&b);
 scanf("%f",&c);
 scanf("%f",&d);
 sum=10*a+5*b+2*c+d;
 printf("Total amount in the piggybank=%0.2f",sum);

	return 0;
}