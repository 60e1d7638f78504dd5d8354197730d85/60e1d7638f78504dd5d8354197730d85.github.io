#include <stdio.h>
int main()
{ int ten; int five; int two; int one;double a;double b; double c; double d; double e;
  scanf("%d%d%d%d",&ten,&five,&two,&one);
 a=10*ten;
 b=5*five;
 c=2*two;
 d=1*one;
 e=a+b+c+d;
 printf("Total amount in the piggybank=%.2f",e);
 
  return 0;
}