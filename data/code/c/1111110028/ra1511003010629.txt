#include <stdio.h>
int main()
{
float a,b,n,m,sum;
  scanf("%f",&a);
  scanf("%f",&b);
  scanf("%f",&n);
  scanf("%f",&m);
  sum=10*a+5*b+2*n+1*m;
  printf("Total amount in the piggybank=%.2f",sum);
  
	return 0;
}