#include <stdio.h>
int main()
{

  int ten,five,two,one;
   float sum;
   scanf("%d \n %d \n %d \n %d",&ten,&five,&two,&one);
  sum= sum +(10*ten) +(5*five) +(2*two) +one;
   printf("Total amount in the piggybank=%.2f",sum);
  
  
  
	return 0;
}