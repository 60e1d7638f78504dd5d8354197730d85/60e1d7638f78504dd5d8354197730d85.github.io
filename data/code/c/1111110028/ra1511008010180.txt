#include <stdio.h>
int main()
{
int a=10,b=5,c=2,d=1;
  int p,q,r,s;
  scanf("%d %d %d %d",&p,&q,&r,&s);
  float sum= a*p+b*q+c*r+d*s;
  printf("Total amount in the piggybank=%.2f",sum);
  
	return 0;
}