#include <stdio.h>
int main()
{
  int t,f,tw,o;
  float c;
  scanf("%d",&t);
  scanf("%d",&f);
  scanf("%d",&tw);
  scanf("%d",&o);
  c=((10*t)+(5*f)+(tw*2)+(o*1));
 printf("Total amount in the piggybank=%.2f",c);
	return 0;
}