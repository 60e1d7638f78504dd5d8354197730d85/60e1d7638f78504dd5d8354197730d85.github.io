#include <stdio.h>
int main()
{
  int n,i,sum=0,a,b,c,d;
  scanf("%d %d %d %d",&a,&b,&c,&d);
  a=10*a;
  b=5*b;
  c=2*c;
  d=1*d;
 
  printf("Total amount in the piggybank=%d.00",a+b+c+d);

	return 0;
}