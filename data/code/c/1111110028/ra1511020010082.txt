#include <stdio.h>
//#include <conio.h>
int main()
{
int a,b,c,sum,sums,sumes,d;
  double result=0;
  scanf("%d",&a);
  scanf("%d",&b);
  scanf("%d",&c);
  scanf("%d",&d);
  sum=(a*10);
  sums=(b*5);
  sumes=(c*2);
  result=sum+sums+sumes+d;
  printf("Total amount in the piggybank=%.2lf",result);
  
  
  
  
	return 0;
}