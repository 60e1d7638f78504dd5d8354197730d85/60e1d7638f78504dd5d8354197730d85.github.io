#include <stdio.h>
int main()
{  int ten, five, two, one;
 float sum=0;
 int t,f,tw,o;
   scanf("%d",&ten);
 scanf("%d", &five);
 scanf("%d", &two);
 scanf("%d",&one);
 t= ten*10;
 f= five*5;
 tw= two*2;
 o=one*1;
 sum=t+f+tw+o;
 printf("Total amount in the piggybank=%.2f", sum);

	return 0;
}