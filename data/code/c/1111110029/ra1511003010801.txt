#include <stdio.h>
#include <math.h>
int main()
{
	int vi=0;
  float vf,a=9.8,d,m;
  	scanf("%f",&d);
  	m=a*d;
  	vf=sqrt(vi+2*m);
  	printf("The object will hit the ground at %0.2f m/s",vf);
	return 0;
}