#include <stdio.h>
#include<math.h>

int main()
{
  int vi=0;
  int heigth;
   scanf("%d",&heigth);
   float vf=sqrt(vi+(2*9.8*heigth));
  
    printf("The object will hit the ground at %.2f m/s",vf);
	return 0;
}