#include <stdio.h>
#include<math.h>
int main()
{
int d;
 float a=9.8,v1,v2;
  
  scanf("%d",&d);
    
  v1=0;
  v2=sqrt(v1=2*a*d);
  printf("The object will hit the ground at %0.2f m/s ",v2);
	return 0;
}