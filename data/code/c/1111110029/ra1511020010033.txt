#include <stdio.h>
#include<math.h>
int main()
{
  float vf;
  int vi=0;
  float a=9.8;
  int d;
  
  scanf("%d",&d);
  vf= sqrt(vi+2*(a*d));
  printf("The object will hit the ground at %0.2f m/s",vf);

	return 0;
}