#include <stdio.h>
#include <math.h>
int main()
{
 int meter,vi=0,d;
  float a=9.8,vf;
  scanf("%d",&d);
  vf=sqrt(vi+2*a*d);
        printf("The object will hit the ground at %0.2f m/s",vf);
   
	return 0;
}