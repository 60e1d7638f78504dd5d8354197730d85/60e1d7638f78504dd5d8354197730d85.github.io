#include <stdio.h>
void convert(int,int);
int main()
{
int num;
scanf("%d",&num);
  printf("\nDecimal value:");
 
  //float k = num;
  //printf("%.2f",k);
  
  convert(num,10);
  //printf("\n");
  printf("\nOctal value:");
  convert(num,8);
  //printf("\n");
  printf("\nHexadecimal value:");
  convert(num,16);
  //printf("\n");
  
return 0;
}

void convert(int num, int base)
{
  int rem = num%base;
  if(num==0)
    return;
  convert(num/base,base);
  
  if(rem<10)
    printf("%d",rem);
  else
    printf("%c",rem-10+'a');
}