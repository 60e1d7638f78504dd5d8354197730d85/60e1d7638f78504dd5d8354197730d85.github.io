#include <stdio.h>

int main()
{
    long long num;
    int count = 0;

    scanf("%lld", &num);

    /* Run loop till num is greater than 0 */
    while(num != 0)
    {
       
        count++;

       
        num /= 10;
    }

    printf(" %d", count);

    return 0;
}