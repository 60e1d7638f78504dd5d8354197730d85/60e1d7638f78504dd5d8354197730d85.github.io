#include <stdio.h>
#include<math.h>
int main()
{
  int a,b,c;
  float s=0,area=0;
  scanf("%d %d %d",&a,&b,&c);
  s=(float)(a+b+c)/2;
  area=sqrt(s*(s-a)*(s-b)*(s-c));
  printf("Area=%.4f",area);

	return 0;
}