#include <stdio.h>
int main()
{
  int i,sum,n;
  sum=0;
  scanf("%d",&n);
  
  for(i=1;i<=n;i++)
  {
    sum+=i;
    
    printf("%d",i);
    if(i<n)
      printf("+");
  }
   printf("=%d ",sum);
 
	return 0;
}