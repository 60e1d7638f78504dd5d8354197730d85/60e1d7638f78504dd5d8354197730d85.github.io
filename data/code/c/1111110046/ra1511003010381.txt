#include <stdio.h>
int main()
{
    int number, i;
  int sum=0;
    scanf("%d",&number);
    for(i=1; i <= number; ++i)
    {
        if (number%i == 0)
        {
            printf("%d ",i);
          	sum=sum+i;
        }
    }
  printf("\n%d",sum);

    return 0;
}

