#include<stdio.h> // place this '<' & '>' instead of '(' & ')' before stdio.h

int main( )
{
int no, sum=0, x;


scanf("%d", &no);
for( x=1; x<=no; x++) {
if(no%x==0) sum=sum+x;
}
  for(x=1; x<= no; ++x)
    {
        if (no%x == 0)
        {
            printf("%d ",x);
        }
  }
printf("\nSum=%d", sum);
return 0;
}
