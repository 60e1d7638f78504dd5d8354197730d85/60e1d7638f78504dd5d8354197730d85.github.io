#include <stdio.h>
int main()
{
int n,hr,min,sec;
	
	scanf("%d",&n);
	if(n>3600){
		min = n/60;
		sec = n%60;
		hr=min/60;
		min = min%60;
		printf(" %d %d %d ",hr,min,sec);
	}
	else{
      if(n==3600)
        printf("%d %d %d",1,0,0);
      else
      {
		min = n/60;
		sec = n%60;
         hr=min/60;
		printf("%d %d %d ",hr,min,sec);
    
      }
    }
	return 0;
}