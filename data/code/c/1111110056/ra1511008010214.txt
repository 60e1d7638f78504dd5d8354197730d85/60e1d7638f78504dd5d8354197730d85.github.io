#include <stdio.h>
int main()
{
int a,b;
  //printf("enter the no of banles collected by malu:");
  scanf("%d",&a);
  //printf("enter the no of bangles collected by doss :");
  scanf("%d",&b);
  if(a>b)
    printf("%d",a);
  else
    printf("%d",b);
	return 0;
}