#include <stdio.h>
int main()
{
  char txt[20];
  int i;
  scanf("%c",&txt[i]);
  if(isalpha(txt[0]))
     {
       printf("valid identifier");
     }
     else
     {
       printf("not a valid identifier");
     }

	return 0;
}