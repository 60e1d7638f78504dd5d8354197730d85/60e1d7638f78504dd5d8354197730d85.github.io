#include <stdio.h>
#include<math.h>
int main()
{int num;
    int a, n;
    scanf("%d", &n);
    for(a = 0; a <= n; a++)
    {
        if (n == a * a)
        {
            printf("TRUE");
            return 0;
        }
    }
    printf("FALSE");
  
	return 0;
}