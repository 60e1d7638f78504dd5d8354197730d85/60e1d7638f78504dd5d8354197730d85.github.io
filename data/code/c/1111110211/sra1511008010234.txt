#include <stdio.h>
int sum(int,int);
int main()
{
  int t,a,b;
  scanf("%d",&t);
  while(t--)
  {
    scanf("%d%d",&a,&b);
    if(a==1)
    {
      for(int i=1;i<=b;i++)
        sum1+=i;
    }
    else
    {
      
    }
  }
	
	return 0;
}
