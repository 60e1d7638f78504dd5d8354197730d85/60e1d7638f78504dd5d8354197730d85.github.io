#include <stdio.h>
int main()
{
int age1,age2;
  scanf("%d",&age1);
  scanf("%d",&age2);
  if(age1<age2)
  {
  printf("College 1 is older");
  }
  else if(age1>age2)
  {
  printf("College 2 is older");
  }
	return 0;
}