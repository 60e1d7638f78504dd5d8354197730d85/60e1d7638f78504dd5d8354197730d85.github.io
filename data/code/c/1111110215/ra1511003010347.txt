#include <stdio.h>
int main()
{
  int n,i,arr[100],s1=0,s2=0;
  scanf("%d",&n);
  for(i=0;i<n;i++)
  {
    scanf("%d",&arr[i]);
    if(arr[i]<=0)
      s2=s2+arr[i];
    else
      s1++;
  }
  printf("%d %d",s1,s2);
  return 0;
}