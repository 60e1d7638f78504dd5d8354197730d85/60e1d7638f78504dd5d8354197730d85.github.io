#include <stdio.h>
int main()
{
int s=0,i,n,a[20],c=0;
  scanf("%d",&n);
  for(i=0;i<n;i++)
  {
    scanf("%d",&a[i]);
  }
  for(i=0;i<n;i++)
      {
        if(a[i]>0)
        {
          c++;
        }
		else
        {
          s=s+a[i];
        }
    
  }
  printf("%d %d",c,s);
	return 0;
}