#include <stdio.h>
#include <math.h>
int main()
{
int n1=0,n2=0,i=0,gcd=0,lcm=0;

  scanf("%d %d",&n1,&n2);
  for(i=1;i<=n1&&i<=n2;i++)
  {
    if(n1%i==0 && n2%i==0)
    {
      gcd=i;
    }
  }
  lcm=(n1*n2)/gcd;
  printf("%d %d",gcd,lcm);
	return 0;
}