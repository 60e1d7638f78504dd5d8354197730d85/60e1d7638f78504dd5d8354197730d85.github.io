#include <stdio.h>
int main()
{
  int a,b,lcm=0,gcd=0,i;
  scanf("%d %d",&a,&b);
  for(i=1; i<=a && i<=b ;i++)
  {
    if(a%i==0 && b%i==0)
      gcd=i;
  }
  lcm=(a*b)/gcd;
  printf("%d %d",gcd,lcm);
	return 0;
}