#include <stdio.h>
#include <string.h>
int main()
{
	int i = 0;
   	char s[20];
  	scanf("%s",s);
  	for(i;i<=strlen(s);i++){
    	if(s[i] >= 65 && s[i] <= 92)
          s[i] = s[i] + 32;
      	else if(s[i] >=97 && s[i] <= 124)
          s[i] = s[i] - 32;
    }	
 	printf("%s",s);
  	return 0;
}