#include <stdio.h>
int main()
{
  char str[50],temp;
  int i,j;
  for(i=0;str[i];i++)
  {
  scanf("%s",&str[i]);
  }
  for(i=0;str[i];i++)
  {
    for(j=i+1;str[j];j++)
    {
      if(str[j]<str[i])
      {
        temp=str[j];
        str[j]=str[i];
        str[i]=temp;
      }
    }
  }
printf("%s\n",str);
	return 0;
}