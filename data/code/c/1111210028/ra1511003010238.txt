#include <stdio.h>
int main()
{
  int i,n,rem,sum=0;
  scanf("%d",&n);
  for(i=1;i<=(n-1);i++)
  {
    rem=n%i;
    if(rem==0)
    {
      sum=sum+i;
    }
  }
  if(sum==n)
    printf("Perfect number");
  else
    printf("Not a perfect number");
	return 0;
}