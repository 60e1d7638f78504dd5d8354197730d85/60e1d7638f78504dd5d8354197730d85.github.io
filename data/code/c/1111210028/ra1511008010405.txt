#include <stdio.h>
int main()
{
    int i,s=0,n;
    scanf("%d",&n);
  for(i=1;i<n;i++){
   if(n%i==0)
     s+=i;
  }
  //printf("%d",s);
  if(s==n)
    printf("Perfect number");
  else
    printf("Not a perfect number");
	return 0;
}