#include <stdio.h>
int main()
{
	int x,y,z;
  	scanf("%d %d %d",&x,&y,&z);
  if(z<x && z<y)
    printf("impossible");
  	else if(z%x==0 && z%y==0)
      printf("strong");
  else if(z%x==0 || z%y==0)
    	printf("accept");
  else
    printf("reject");
	return 0;
}