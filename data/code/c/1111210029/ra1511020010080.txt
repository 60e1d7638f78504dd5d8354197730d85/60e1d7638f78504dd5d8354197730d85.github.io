#include <stdio.h>
int main()
{

  int x,y, k;
  
  scanf("%d %d",&x,&y);
  scanf("%d",&k);
  
  if(k%x==0 && k%y==0)
    printf("strong");
  else if(k%x==0 || k%y==0)
    printf("accept");
  else
    printf("reject");
  
  
	return 0;
}
