#include <stdio.h>
int main()
{int n,i,sum=0;
 scanf("%d",&n);
 for(i=1;i<=n;i++)
 {
   sum=sum+i*i*i;
   printf("%d^3",i);
   if(i==n)
     break;
   else
     printf(" + ");
 }
 printf("=%d",sum);
	return 0;
}