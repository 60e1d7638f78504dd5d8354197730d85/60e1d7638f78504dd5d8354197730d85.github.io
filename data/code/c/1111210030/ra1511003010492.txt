#include <stdio.h>
#include<math.h>
int main()
{
int n,sum=0,i;
scanf("%d",&n);
 for(i=1;i<=n;i++)
 {
    printf("%d^3",i);
 if(i!=n)
 {
 printf(" + ");
 }
 sum+=pow(i,3);
   
 }
 printf("=%d",sum); 
	return 0;
}