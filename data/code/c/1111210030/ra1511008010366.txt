#include <stdio.h>
#include<math.h>
int main()
{
	int n,i,sum=0;
  scanf("%d",&n);
  for(i=1;i<n;i++)
  {
    sum+=pow(i,3);
    printf("%d^3 + ",i);
  }
  sum+=pow(i,3);
    printf("%d^3=%d",i,sum);
	return 0;
}