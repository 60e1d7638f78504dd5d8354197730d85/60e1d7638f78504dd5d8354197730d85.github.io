#include <stdio.h>
int main()
{
int mark;
scanf("%d",&mark);
if((mark>=75)&&(mark<=100))
  printf("GRADE A");
else if((mark>=60)&&(mark<75))
  printf("GRADE B");
else if((mark>=50)&&(mark<60))
  printf("GRADE C");
else if((mark>=0)&&(mark<49))
  printf("FAIL");
else
  printf("ERROR");
	return 0;
}