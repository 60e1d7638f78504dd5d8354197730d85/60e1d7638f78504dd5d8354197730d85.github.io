#include <stdio.h>
int main()
{
  int num, rev=0, rem;
  scanf("%d", &num);
  int temp=num;
  while(num >0){
    rem=num%10;
    rev= rev*10 +rem;
    num= num/10;
  }
  num=temp;
  if(rev>num)
    printf("%d", num+rev);
  else
    printf("impossible");
    
	return 0;
}