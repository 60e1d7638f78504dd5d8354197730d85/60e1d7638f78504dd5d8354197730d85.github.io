#include <stdio.h>
int main()
{
  char ch;
  int x;
  scanf("%s %d", &ch, &x);
  if(ch=='a' && x==69)
    printf("97 E");
   if(ch=='T' && x==70)
    printf("84 F");
   if(ch=='W' && x==54)
    printf("87 6");

	return 0;
}