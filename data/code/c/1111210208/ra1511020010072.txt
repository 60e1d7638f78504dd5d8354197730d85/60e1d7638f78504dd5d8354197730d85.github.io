#include <stdio.h>
int main()
{
    char c;
  
  int  ch;
   // printf("Enter a character: ");

    // Reads character input from the user
    scanf("%c %d", &c,&ch);  
    
   
    printf("%d ", c);
   printf("%c",ch);
    return 0;
}