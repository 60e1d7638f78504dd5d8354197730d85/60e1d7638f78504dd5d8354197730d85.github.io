#include <stdio.h>
int main()
{
	int n,arr[1000],i,j;
  int sum=0;
  	scanf("%d",&n);
  	for(i=0;i<n;i++)
      scanf("%d",&arr[i]);
  	for(i=0;i<n;i++){
    for(j=i+1;j<n;j++){
    sum=sum+(arr[i] & arr[j]);
    }
    }
  printf("%d",sum);
	return 0;
}