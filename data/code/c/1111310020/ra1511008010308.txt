#include<stdio.h>
int main()
{
  int i,sum=0,j,n;
  scanf("%d",&n);
  int a[n];
  for(i=0;i<n;i++)
  {
    scanf("%d",&a[i]);
  }
  for(i=0;i<n;i++)
  {
	for(j=i+1;j<n;j++)
    {
      sum=sum+(a[i]&a[j]);
    }
  }
  printf("%d",sum);
  return 0;
}