#include <stdio.h>
int main()
{
  	int n, x, i, score = 0, flag =0;
  	scanf("%d %d",&n,&x);
  	int a[n];
  	for(i=0;i<n;i++)
  		scanf("%d",&a[i]);
	
  	for(i=0;i<n;i++)
      if(a[i]<=x)
        score++;
  	  else if(flag==0)
      	flag=1;
  	  else
        break;
    printf("%d",score);
	return 0;
}