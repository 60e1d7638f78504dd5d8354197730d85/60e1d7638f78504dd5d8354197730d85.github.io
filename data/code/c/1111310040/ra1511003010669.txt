#include <stdio.h>
int main()
{
  	int n,i,sum=0;
	scanf("%d",&n);
  	int a[n];
  	for(i=0;i<n;i++)
    {
      scanf("%d",&a[i]);
      if(i>0&&a[i]==0)
        a[i-1]=0;
    }
  	for(i=0;i<n;i++)
      sum+=a[i];
  	printf("%d",sum);
	return 0;
}