#include <stdio.h>
int main()
{
  int x,y,sum;
  float m,n,s;
  scanf("%d",&x);
  scanf("%d",&y);
  sum=x+y;
  scanf("%f",&m);
  scanf("%f",&n);
  s=m+n;
  printf("The Addition of two number is:%d\n",sum);
  printf("The Addition of two number is:%0.2f\n",s);
  

	return 0;
}