#include <stdio.h>
int main()
{
	float x,y;
  	int a,b;
  	scanf("%d%d",&a,&b);
  	scanf("%f%f",&x,&y);
  	printf("The Addition of two number is:%d",a+b);
  	printf("\nThe Addition of two number is:%.2f",x+y);
	return 0;
}