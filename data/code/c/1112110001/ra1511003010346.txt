#include <stdio.h>
int main()
{
int a,b;
  float x,y;
  scanf("%d%d",&a,&b);
  scanf("%f%f",&x,&y);
  printf("The Addition of two number is:%d\n",(a+b));
  printf("The Addition of two number is:%.2f",(x+y));
	return 0;
}