#include <stdio.h>
int main()
{
  int a,b;
  float x,y;
  scanf("%d %d %f %f",&a,&b,&x,&y);
  printf("The Addition of two number is:%d\n",a+b);
  printf("The Addition of two number is:%0.2f",x+y);

	return 0;
}