#include <stdio.h>
int main()
{
  int x,y;
  float p,q;
  scanf("%d %d",&x,&y);
  scanf("%f %f",&p,&q);
  printf("The Addition of two number is=%d\n",x+y);
  printf("The Addition of two number is=%.1f",p+q);

	return 0;
}