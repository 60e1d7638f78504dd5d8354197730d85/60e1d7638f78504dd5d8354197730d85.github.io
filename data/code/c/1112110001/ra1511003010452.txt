#include <stdio.h>
int main()
{ int a,b,remain;
  float c,d,add;
  scanf("%d",&a);
  scanf("%d",&b);
  scanf("%f",&c);
 scanf("%f",&d);
  remain=a+b;
  add=c+d;
 printf("The Addition of two number is:%d\n",remain);
 printf("The Addition of two number is:%.2f",add);

	return 0;
}