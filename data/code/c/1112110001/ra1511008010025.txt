#include <stdio.h>
int main()
{
	int a,s;
    float z,x;
  	scanf("%d", &a);
  scanf("%d", &s);
  scanf("%f", &z);
  scanf("%f", &x);
  printf("The Addition of two number is:%d",a+s);
  printf("\nThe Addition of two number is:%0.2f",z+x);
	return 0;
}