#include <stdio.h>
int main()
{
int a,b,s;
float c,d,sum;
    scanf("%d",&a);
    scanf("%d",&b);
        scanf("%f",&c);
        scanf("%f",&d);
        s=a+b;
        sum=c+d;
  printf("\nThe Addition of two number is:%d",s);
  printf("\nThe Addition of two number is:%.2f",sum);
	return 0;
}