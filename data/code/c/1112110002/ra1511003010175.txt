#include <stdio.h>
int main()
{
    int one, two, div ;
    
    
    
    scanf("%d", &one);
   
    scanf("%d", &two);
  div = one / two;
   
    printf("The Division of two number is:%d",div);
    return 0;
}