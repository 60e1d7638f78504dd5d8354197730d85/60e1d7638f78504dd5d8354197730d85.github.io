#include <stdio.h>
int main()
{
  int firstnumber, secondnumber, division;
  scanf("%d %d", &firstnumber, &secondnumber);
  division = firstnumber/secondnumber;
  printf("The Division of two number is=%d", division);
  return 0;
}