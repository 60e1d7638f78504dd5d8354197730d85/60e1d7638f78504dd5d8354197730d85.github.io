#include <stdio.h>
#include<math.h>
int main()
{
float a,b,sol;
  scanf("%f",&a);
  scanf("%f",&b);
  sol=(a/b);
int c;
  c=floor(sol);
  printf("The Division of two number is:%d",c); 
  
	return 0;
}