#include <stdio.h>
int main()
{
  double a;
  double b;
  scanf ("%lf", &a);
  scanf ("%lf", &b);
  printf ("The Subtraction of two number is:%lf\n", (a-b));
  printf ("The Subtraction of two number is:%0.2lf", (a-b));
	return 0;
}