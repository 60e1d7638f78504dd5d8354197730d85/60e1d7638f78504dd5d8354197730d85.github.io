#include <stdio.h>
int main()
{
  float a,b,m;
  scanf("%f",&a);
  scanf("%f",&b);
if(a>b)
  {
    m=a-b;
       }
    printf("The Subtraction of two number is:%f\n",m);
    printf("The Subtraction of two number is:%.2f",m);
	return 0;
}