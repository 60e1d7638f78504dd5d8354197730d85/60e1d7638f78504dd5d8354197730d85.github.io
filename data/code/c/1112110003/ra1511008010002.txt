#include <stdio.h>
int main()
{
float a,b;
  scanf("%f",&a);
    scanf("%f",&b);
  float diff;
  diff = a-b;
  printf("The Subtraction of two number is:%.6f",diff);
  printf("\nThe Subtraction of two number is:%.2f",diff);
	return 0;
}