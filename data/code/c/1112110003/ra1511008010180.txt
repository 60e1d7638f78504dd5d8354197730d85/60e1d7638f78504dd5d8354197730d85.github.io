#include <stdio.h>
int main()
{
float a, b;
  float p,q;
  scanf("%f",&p);
  scanf("%f",&q);
  a=p-q;
  b=p-q;
    printf("The Subtraction of two number is:%.6f",a);
      printf("\nThe Subtraction of two number is:%.2f",b);

  
	return 0;
}