#include <stdio.h>
int main()
{
float a,b,diff;
  scanf("%f %f",&a,&b);
  diff=a-b;
  printf("The Subtraction of two number is:%f\n",diff);
  printf("The Subtraction of two number is:%0.2f",diff);
  
	return 0;
}