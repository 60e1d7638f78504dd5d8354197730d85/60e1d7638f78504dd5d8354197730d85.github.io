#include <stdio.h>
int main()
{
    float n,k,s;
    scanf("%f %f",&n,&k);
    s=n-k;
    printf("The Subtraction of two number is:%f",s);
    printf("\nThe Subtraction of two number is:%.2f",s);
	return 0;
}