#include <stdio.h>
int main()
{ 
  double a,b;
  scanf("%lf %lf",&a,&b);
  printf("The Subtraction of two number is:%lf\n",(a-b));
  printf("The Subtraction of two number is:%.2lf",(a-b));
	return 0;
}