#include <stdio.h>
int main()
{
float one, two, sub;

printf(" ");

scanf("%f",&one);

printf(" ");

scanf("%f",&two);

sub = one - two;

printf("\nThe Subtraction of two number is:%f",sub);
  printf("\nThe Subtraction of two number is:%.2f",sub);
   
  return 0;
}
