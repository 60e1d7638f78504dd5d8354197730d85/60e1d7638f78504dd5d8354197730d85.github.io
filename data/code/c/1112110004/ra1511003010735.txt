#include <stdio.h>
#include <math.h>
int main()
{
  float a, b;
  scanf("%f\n%f", &a, &b);
  printf("The Multiplication of two number is:%0.6f\n", (a*b));
  printf("The Multiplication of two number is:%0.2f", (a*b));

	return 0;
}