#include <stdio.h>
int main()
{
float p,q;
  scanf("%f", &p);
  scanf("%f", &q);
  printf("The Multiplication of two number is:%0.6f\n", (p*q));
  printf("The Multiplication of two number is:%0.2f\n", (p*q));
	return 0;
}