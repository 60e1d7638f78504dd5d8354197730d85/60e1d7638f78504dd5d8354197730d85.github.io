#include <stdio.h>
int main()
{
  double a;
  float b;
  scanf("%lf %f",&a,&b);
  printf("The Multiplication of two number is:%f\n",(a*b));
  printf("The Multiplication of two number is:%0.2f",(a*b));

	return 0;
}