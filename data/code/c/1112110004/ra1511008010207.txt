#include <stdio.h>
#include <math.h>
int main()
{    float a,b,c,d;
     scanf("%f%f",&a,&b);
    c = a*b;
    d = a*b;
    printf("The Multiplication of two number is:%f\n",c);
    printf("The Multiplication of two number is:%.2f\n",d);

	return 0;
}