#include <stdio.h>
int main()
{int a,b,c;
 scanf("%d%d",&a,&b);
 c=a % b;
 if(c==0)
 {
   printf("The modulo of two number is:0");
 }
else
{
  printf("The modulo of two number is:%d",c);
}
	return 0;
}