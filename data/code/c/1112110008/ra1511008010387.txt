#include <stdio.h>
int main()
{

	int one, two, multiply;
printf(" ");
scanf("%d",&one);
printf(" ");
scanf("%d",&two);
multiply = one * two;;
printf("The Multiplication of two number is:%d",multiply);
  return 0;
}