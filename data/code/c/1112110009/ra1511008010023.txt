#include <stdio.h>
 
int main()
{
   int first, second,divide;
   scanf("%d%d", &first, &second);
   divide = first / second;  
 
   
   printf("The division of two number is:%d\n",divide);
 
   return 0;
}