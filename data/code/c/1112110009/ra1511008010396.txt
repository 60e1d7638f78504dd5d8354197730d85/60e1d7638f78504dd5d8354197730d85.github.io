#include <stdio.h>
int main()
{
	int a,b;
  scanf("%d%d",&a,&b);
  if(a>=b)
    printf("The division of two number is:%d",(a/b));
  if(a<b)
    printf("The division of two number is:%d",(b/a));
	return 0;
}