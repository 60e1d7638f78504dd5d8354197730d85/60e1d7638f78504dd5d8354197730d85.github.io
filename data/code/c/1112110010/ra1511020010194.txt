#include <stdio.h>
int main()
{
int n,m,mod;
 // printf("Enter first number:");
  scanf("%d",&n);
 // printf("Enter second number :");
  scanf("%d",&m);
  mod=n%m;
  printf("The reminder of two number is:%d",mod);
	return 0;
}