#include <stdio.h>
#include<math.h>
int main()
{
  float a,b;
  scanf("%f",&a);
  scanf("%f",&b);
  //int c= (int) a;
  //int d= (int) b;
  printf("Value is:%0.0f",round(a));
  printf("\nValue is:%0.0f",round(b));
  

	return 0;
}