#include <stdio.h>
#include <math.h>

int main()
{
  float f,g;
  int c,d;
  scanf("%f %f",&f,&g);
  c=(int)(round(f));
  d=(int)(round(g));
  printf("Value is:%d",c);
  printf("\nValue is:%d",d);
 
  

	return 0;
}