#include <stdio.h>
#include <math.h>
int main()
{
    int t,k,i;
 float n; 
 // scanf("%d",&t);
  for(i=0;i<2;i++)
  {
    scanf("%f",&n);
    k=(int)(round(n));
    printf("Value is:%d\n",k);
    k=0; }
	return 0;
}