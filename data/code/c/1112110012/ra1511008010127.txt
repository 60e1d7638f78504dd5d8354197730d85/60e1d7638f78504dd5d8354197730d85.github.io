#include <stdio.h>
#include <math.h>
int main()
{
  float num,num1;
  int d,e;
    scanf("%f",&num);
    scanf("%f",&num1);
    e=round(num1);
    d=round(num);
    printf("Value is:%d\n",d);
    printf("Value is:%d",e);
	return 0;
}