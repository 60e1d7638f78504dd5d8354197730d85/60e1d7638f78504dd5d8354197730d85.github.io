#include <stdio.h>
#include <math.h>
int main()
{
  	float n, m;
	scanf("%f", &n);
  	scanf("%f", &m);
  	int x = round(n);
  	int y = round(m);
  	printf("Value is:%d\n", x);
  	printf("Value is:%d", y);
	return 0;
}