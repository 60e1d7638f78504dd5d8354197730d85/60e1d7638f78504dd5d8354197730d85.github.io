#include <stdio.h>
#include<math.h>
#include<stdlib.h>
int main()
{
	float a,b;
  	scanf("%f %f",&a,&b);
  	int x=(float) a+0.5;
  	int y=(float) b+0.5;
  	printf("Value is:%d",x);
    printf("\nValue is:%d",y);
	return 0;
}