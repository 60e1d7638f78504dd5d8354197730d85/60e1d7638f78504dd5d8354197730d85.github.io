#include <stdio.h>
#include <math.h>
int main()
{float p1,p2;
 scanf("%f",&p1);
 scanf("%f",&p2);
int a1=round(p1);
 int a2=round(p2);
 printf("Value is:%d",a1);
  printf("\nValue is:%d",a2);

	return 0;
}