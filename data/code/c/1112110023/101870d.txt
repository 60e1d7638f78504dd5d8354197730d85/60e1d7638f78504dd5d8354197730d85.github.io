#include <stdio.h>
int main()
{
  int x , y;
  scanf("%d",&x);
  scanf("%d",&y);
  x = x + y;  
  y = x - y;  
  x = x - y;  
  printf("\nValues after Swapping");
  printf("\nvalue of a is:%d",x);
  printf("\nvalue of b is:%d",y);
return 0;
}