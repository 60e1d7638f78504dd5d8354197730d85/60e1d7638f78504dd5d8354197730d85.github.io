#include <stdio.h>
int main()
{
  int r;
  scanf ("%d", &r);
  int s;
  scanf ("%d", &s);
  if ((22/7*r*r)>(s*s))
    printf("I prefer centre 1");
  else
    printf("I prefer centre 2");

	return 0;
}