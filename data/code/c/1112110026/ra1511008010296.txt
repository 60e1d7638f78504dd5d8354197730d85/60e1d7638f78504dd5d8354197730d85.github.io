#include <stdio.h>
 
int main()
{
   float first, second;
   float divide;
   scanf("%f %f", &first, &second);
   divide = first / second; 
    printf("The Division of two number is:%f\n",divide);
 
   return 0;
}