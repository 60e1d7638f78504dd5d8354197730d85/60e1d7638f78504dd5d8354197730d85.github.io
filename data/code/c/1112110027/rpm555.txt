#include <stdio.h>
#include <string.h>
int main()
{
int num,digits;
  char ch[20];
  scanf("%d",&num);
  sprintf(ch,"%d",num);
  digits=strlen(ch);
  printf("\n Number of Digits:%d",digits);
  
	return 0;
}