#include <stdio.h>
int main()
{
  int n,i,j,k;
  scanf("%d", &n);
  for(i=0;i<n;i++)
  {
    for(j=0, k=n;j<n;j++, k--)
    {
      if(i+j==n-1)
        printf("*");
      else
        printf("%d",k);
    }
  printf("\n");
  }
  return 0;
}