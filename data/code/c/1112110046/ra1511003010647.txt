#include <stdio.h>
int main()
{
  int n, i, j, x;
  scanf("%d",&n);
  for(i = 0; i < n; i++)
  {
    x = 1;
    for(j = 0; j <= i; j++)
    {
      printf("%d ",x+j);
    }
    printf("\n");
  }
  return 0;
}