#include <stdio.h>
int fib(int);
int main()
{
int n,c,i;
scanf("%d", &n);  
for(i=0;i<n;i++)
{
 printf("%d\n",fib(c));
  c++;
}
  return 0;
}
int fib(int n)
{
if(n==0)
{
  return 0;
}
  if(n==1)
 {
   return 1;
 }
   else
   {
     return (fib(n-1)+fib(n-2));
   }
 }
  
  