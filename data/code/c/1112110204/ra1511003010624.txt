#include<stdio.h>
int lcm(int a,int b)
{
 
    int max=a>b?a:b;
     int x=max;
    while(1)
    {
        if(max%a==0&&max%b==0)
         {
             return max;
         }
               max=max+x;
    }
}
int hcf(int a,int b)
{
    if(a%b==0)
         return b;
    else
        return hcf(b,a%b);
 
}
int main()
{
    int t,x,i,y;
    int a,b;
    scanf("%d",&t);
    while(t--)
    {
        scanf("%d %d",&a,&b);
        x=hcf(a,b);
        y=a*b/x;        printf("%d %d\n",x,y);
 
    }
return 0;
} 