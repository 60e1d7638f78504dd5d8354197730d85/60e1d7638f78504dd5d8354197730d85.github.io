#include <stdio.h>
 
int main(){
	int t;
	int n1, n2, m, n;
	scanf ("%d", &t);
	
	while (t--) {
	    scanf ("%d %d", &n1, &n2);
	    m = n1;
	    n = n2;
	    
	    while (n) {
	        m = m % n;
	        m ^= n ^= m ^= n;
	    }
	    
	    printf ("%d %d\n", m, (n1 * n2) / m);
	}
	return 0;
}  