#include <stdio.h>
int main()
{
  int g_num,i,j;
  scanf("%d",&g_num);
  for(i=2;i<=g_num;i++)
  {
    int flag=0;
    for(j=2;j<=i/2;++j)
    {
      if(i%j==0)
      {
        flag=1;
        break;
      }
    }
    if(flag==0)
      printf(" %d",i);
  }

	return 0;
}