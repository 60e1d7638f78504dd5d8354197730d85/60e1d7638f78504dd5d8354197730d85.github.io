#include <stdio.h>
int main()
{
int num,digit, sum = 0;
 
    scanf("%d", &num);
    
    while (num > 0)
    {
        digit = num % 10;
        sum  = sum + digit;
        num /= 10;
    }
    printf("Sum of the Digits:%d\n", sum);
	return 0;
}