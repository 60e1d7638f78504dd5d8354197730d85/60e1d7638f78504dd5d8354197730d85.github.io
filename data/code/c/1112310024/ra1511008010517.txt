#include <stdio.h>
int main()
{
  int t,i,n,cnt=0;
  scanf("%d",&t);
  
  while(t--){
    cnt=0;
    
    scanf("%d",&n);
    int arr[n];
    
    for(i=0;i<n;i++)
    {
      scanf("%d",&arr[i]);
      if(arr[i]%2==0)
        cnt++;
    }
    printf("%d\n",cnt);
  }

	return 0;
}