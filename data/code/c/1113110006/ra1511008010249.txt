#include <stdio.h>
int main()
{
 int l,b,w,q;
  scanf("%d",&l);
    scanf("%d",&b);
  w=(2*l)+(2*b);
  q=(l*b);
  printf("Required length is %dm\n",w);
  printf("Required quantity of carpet is %dsqm\n",q);
  
	return 0;
}