#include <stdio.h>
int main()
{
  int a,b,p,area;
  scanf("%d",&a);
  scanf("%d",&b);
  p=2*(a+b);
  area=a*b;
  printf("Required length is %d",p);
  printf("m\n");
  printf("Required quantity of carpet is %d",area);
  printf("sqm");
  

	return 0;
}