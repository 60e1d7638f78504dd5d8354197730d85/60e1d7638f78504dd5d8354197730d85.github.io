#include <stdio.h>
int main()
{
int len,br;
  scanf("%d" "%d",&len,&br);
  int per=2*(len+br);
  int ar=len*br;
  printf("Required length is %dm\n",per);
  printf("Required quantity of carpet is %dsqm\n",ar);
	return 0;
}