#include <stdio.h>
int main()
{
  int a,b,l,c;
  scanf("%d %d",&a,&b);
  l=(2*a)+(2*b);
  c=a*b;
  printf("Required length is %dm\n",l);
  printf("Required quantity of carpet is %dsqm",c);

	return 0;
}