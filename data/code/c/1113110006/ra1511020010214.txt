#include <stdio.h>
int main()
{
  int l,b,f,c;
  scanf("%d %d",&l,&b);
  f=2*(l+b);
  c=l*b;
  printf("Required length is %dm\n",f);
  printf("Required quantity of carpet is %dsqm",c);
    

	return 0;
}