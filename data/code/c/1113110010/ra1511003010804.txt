#include <stdio.h>
int main()
{
  
  int n,sum=0,r;
  scanf("%d",&n);
  if(n>=10 && n<=99)
  {
    
    while(n!=0)
    {
    r=n%10;
    sum=sum+r;
    n=n/10;
    }
    printf("Alice must go in path %d",sum);
  }
 else
 {
   printf("not a 2 digit number");
 }
 return 0;
}