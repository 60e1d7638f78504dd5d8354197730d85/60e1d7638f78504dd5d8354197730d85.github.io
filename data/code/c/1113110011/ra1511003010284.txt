#include <stdio.h>
#include <math.h>
int main()
{
  int a, b, c, k;
  float radius;
  scanf("%d %d %d", &a, &b, &c);
  k = (a+b+c)/2;
  radius = sqrt(k*(k-a)*(k-b)*(k-c))/k;
  printf("The radius of the circle is=%0.2f", radius);
  return 0;
}