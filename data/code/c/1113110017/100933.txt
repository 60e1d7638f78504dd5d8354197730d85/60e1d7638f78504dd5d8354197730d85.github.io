#include <stdio.h>
int main()
{
   int first, second, add, subtract;
   float multiply;
   scanf("%d%d", &first, &second);
   multiply = first * second;
   printf("multiplication of the two numbers is %.2f\n",multiply);
   return 0;

}