#include <stdio.h>
int main()
{
 float s1,s2,s3,s4,s5,x,a;
  scanf("%f %f %f %f %f %f",&s1,&s2,&s3,&s4,&s5,&x);
  a=6*x-(s1+s2+s3+s4+s5);
  printf("The required sale in the sixth month is %.2f",a);
  
	return 0;
}