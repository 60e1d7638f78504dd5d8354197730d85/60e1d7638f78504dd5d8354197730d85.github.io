#include <stdio.h>
int main()
{ int x,y,l;int x1,y2;char s='(';
  char ss=')';
  
 scanf("%d%d%d",&x,&y,&l);
 x1=x+(l/2);
  y2=y+(l/2);
  printf("centre of the ground is at%c%d,%d%c",s,x1,y2,ss);
  return 0;

	return 0;
}