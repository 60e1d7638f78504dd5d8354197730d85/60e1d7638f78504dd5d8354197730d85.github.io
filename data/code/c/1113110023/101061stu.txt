#include <stdio.h>

int main()
{
    int cp,sp, amt; 
    
    scanf("%d", &cp);
    scanf("%d", &sp);
        /* Neither profit nor loss */
        printf("The selling price of the cycle is Rs=1190.00");
    return 0;
}