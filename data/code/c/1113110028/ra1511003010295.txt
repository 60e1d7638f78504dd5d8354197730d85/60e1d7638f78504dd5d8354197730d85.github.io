#include <stdio.h>
int main()
{ 
  int a, i;
    scanf("%d", &a);
    int b[a][2];
    for(i=0; i<a; i++)
    {
        scanf("%d %d", &b[i][0], &b[i][1]);
        printf("%d\n", b[i][0]%b[i][1]);
    }
	return 0;
}