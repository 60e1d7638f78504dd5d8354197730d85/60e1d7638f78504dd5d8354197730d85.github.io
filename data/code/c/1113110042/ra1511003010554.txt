#include <stdio.h>
int main()
{
  int i,a,b;
  scanf("%d%d",&a,&b);
  for(i = a;i<b;i++){
    if(i%2==0){
      if(i%3==0 || i%5==0)
      continue;
      else
        printf("%d\n",i);
  }
    
  }
	return 0;
}