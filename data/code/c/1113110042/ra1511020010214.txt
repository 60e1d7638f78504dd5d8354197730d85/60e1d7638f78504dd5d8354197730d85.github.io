#include <stdio.h>
int main()
{

  int a,b;
  scanf("%d %d",&a,&b);
  int i;
  for(i=a;i<=b;i++){
  if(i%2==0 && i%3!=0 && i%5!=0)
  {
    printf("%d\n",i);
  }
    else
      continue;
  }
	return 0;
}