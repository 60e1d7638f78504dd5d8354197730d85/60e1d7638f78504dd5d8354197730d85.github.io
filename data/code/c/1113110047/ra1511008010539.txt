#include <stdio.h>
int main()
{
  int a,b,c,d,sum1,sum2;
    scanf("%d",&a);
    scanf("%d",&b);
    scanf("%d",&c);
    scanf("%d",&d);
  sum1=a+c;
  sum2=b+d;
  printf("%d+%di",sum1,sum2);
	return 0;
}