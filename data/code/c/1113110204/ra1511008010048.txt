#include <stdio.h>
int main()
{
  	int a,b,n;
  	scanf("%d",&n);
  	int i;
  	for(i=0;i<n;i++)
    {
      scanf("%d %d",&a,&b);
      if(a>b)
        printf(">\n");
      else if(a<b)
        printf("<\n");
      else
        printf("=\n");
    }
	return 0;
}