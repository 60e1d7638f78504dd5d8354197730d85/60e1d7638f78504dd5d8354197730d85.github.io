#include<stdio.h>
#include<string.h>
#include<stdlib.h>
int main()
{
    int t,n,i,j,ca,cb;
    char array[5100];
    scanf("%d",&t);
    while(t--)
    {
        scanf("%s",array);
        ca=0;
        cb=0;
        for(i=0;array[i]!='\0';++i)
            if(array[i]=='A')
            ++ca;
        else
            ++cb;
        n=i;
        if(ca==2)
        {
            printf("A\n");
            continue;
        }
        else if(ca==i)
        {
            printf("-1\n");
            continue;
        }
        if(cb==2)
        {
            printf("B\n");
            continue;
        }
        else if(cb==i)
        {
            printf("-1\n");
            continue;
        }
        {
            //if(array[n-1]!=array[n-2])
              //  printf("-1\n");
            //else
            /*if(array[n-1]==array[n-2] && array[n-3]!=array[n-2])
            {
                if(array[n-1]=='A')
                {
                    while(cb--)
                        printf("B");
                    printf("A\n");
                }
                else
                {
                    while(ca--)
                        printf("A");
                    printf("B\n");
                }
            }*/
            j=0;
            for(i=0;array[i+1]!='\0';++i)
            {
                if(array[i]!=array[i+1])
                    ++j;
            }
            if(j>3)
                printf("-1\n");
            else//(j==1 || j==2 || j==3)
            {
                if(j==1 || j==2)
                {
                    if(array[n-1]=='A')
                    {
                    while(cb--)
                        printf("B");
                    printf("A\n");
                    }
                    else
                    {
                    while(ca--)
                        printf("A");
                    printf("B\n");
                    }
                }
                if(j==3)
                {
                    if(array[n-1]=='A')
                    {
                        i=n-1;
                        while(array[i]!='B')
                            --i;
                        while(array[i]!='A')
                            --i;
                        while(array[i]=='A')
                        {
                            printf("A");
                            --i;
                        }
                        printf("B\n");
                    }
                    if(array[n-1]=='B')
                    {
                        i=n-1;
                        while(array[i]!='A')
                            --i;
                        while(array[i]!='B')
                            --i;
                        while(array[i]=='B')
                        {
                            printf("B");
                            --i;
                        }
                        printf("A\n");
                    }
                }
            }
        }
    }
    return 0;
}
 