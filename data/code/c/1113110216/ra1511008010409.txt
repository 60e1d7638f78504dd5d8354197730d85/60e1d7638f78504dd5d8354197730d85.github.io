#include <stdio.h>
void cycle(int * a,int * b,int * c)
{
  int temp;
  temp=*a;
  *a=*c;
  //*b=*c;
  *c=temp;
  
  *c=*b;
  *b=temp;
}
int main()
{
	int a,b,c;
  	scanf("%d %d %d",&a,&b,&c);
  	cycle(&a,&b,&c);
  	printf("%d\n%d\n%d",a,b,c);
	return 0;
}