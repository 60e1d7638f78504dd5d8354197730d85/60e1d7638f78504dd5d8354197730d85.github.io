#include<stdio.h> 
int main()
{
  int n,i,c,sum=0;
   scanf("%d",&n);
  int a[n];
  for(i=0;i<n;i++)
  {
   scanf("%d",&a[i]);
   if(a[i]>=0)
     c++;
   else
     sum=sum+a[i];
  }
  printf("Positive Count=%d\n",c);
  printf("Negative Sum=%d",sum);
  
  return 0;
}