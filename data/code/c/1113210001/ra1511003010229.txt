#include <stdio.h>
int main()
{
  int t;
  scanf("%d",&t);
  while(t--)
  {
int n,a[200],i,cnt=0;
    scanf("%d",&n);
  for(i=1;i<=n;i++)
  {
    if(n%i==0)
    {
      if(i%2==0)
      {
        cnt++;}}}
        printf("%d\n",cnt);
      
  }
	return 0;
}