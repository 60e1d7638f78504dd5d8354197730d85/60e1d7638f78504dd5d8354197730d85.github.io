#include <stdio.h>
#include <math.h>
int main()
{
   int n,count=0,i;
   scanf("%d",&n);
   for(i=0;i<n;i++)
   {
     if(pow(2,i)==n)
     { count=1;
       printf("power series");
       break;
     }
   }
  if(count==0)
    printf("not");
	return 0;
}