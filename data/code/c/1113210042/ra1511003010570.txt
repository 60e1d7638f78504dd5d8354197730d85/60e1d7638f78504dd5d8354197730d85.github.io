#include <stdio.h>

int gcd(int a,int b)
{
  if(b==0)
    return a;
  else
    return gcd(b,a%b);
}

int main()
{

  int a,b;
  scanf("%d",&a);
  scanf("%d",&b);
  int z=gcd(a,b);
  printf("%d",z);
	return 0;
}