#include <stdio.h>
int GCD(int a,int b){
  if(b==0){
    return a;
  }
  else{
    return (GCD(b,(a%b)));
  }
}
int main()
{
  int a,b;
  scanf("%d\n",&a);
  scanf("%d\n",&b);
  int gcd=GCD(a,b);
  printf("%d",gcd);

	return 0;
}