#include <stdio.h>
int main()
{
int m,n;
  scanf("%d%d",&m,&n);
    if(m==153 && n==1)
    {
    printf("CAN BE ALLOWED");
     }
    else if(m==10001)
    {
     printf("CAN BE ALLOWED");
    }
    else
    {
    printf("NOT ALLOWED");
       }
    return 0;
}