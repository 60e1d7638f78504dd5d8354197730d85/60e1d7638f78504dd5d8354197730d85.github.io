#include <stdio.h>
#include<string.h>
int main()
{
  char c;
//  int len;
    char str[50];
   int len;
scanf("%s",str);
   len = strlen(str);
   //printf("Length of |%s| is |%d|\n", str, len);
  
  if(str[len]==c)
  {
    printf("1");
  }
	return 0;
}