#include <stdio.h>
int main()
{
	int n,ans[100];
  	scanf("%d",&n);
  	int i=0;
  	while(n>0){
    	ans[i] = n % 2;
        n = n / 2;
        i++;
    }
  	int j;
  	for (j = i - 1; j >= 0; j--)
        printf("%d",ans[j]);
	return 0;
}