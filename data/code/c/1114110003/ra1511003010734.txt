#include <stdio.h>
int main()
{
 int mincount, maxcount, c;
 
  scanf("%d",&maxcount);
 
  scanf("%d",&mincount);
  c=maxcount/2;
  if(mincount>=c)
    printf("IN");
  else 
    printf("OUT");
  return 0;
	return 0;
}