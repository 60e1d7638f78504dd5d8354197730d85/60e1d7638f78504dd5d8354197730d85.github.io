#include <stdio.h>
int main()
{
int first,second;
  scanf("%d",&first);
  scanf("%d",&second);
  if(first>second)
  {
    printf("College 1 is better");
  }
  else
  {
    printf("College 2 is better");
  }
	return 0;
}