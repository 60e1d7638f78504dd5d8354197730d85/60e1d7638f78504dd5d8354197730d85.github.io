#include <stdio.h>
int main()
{
    int a,b,d1,d2;
    scanf("%d %d",&a,&b);
   d1=2017-a;
   d2=2017-b;
   if(d1>d2)
     printf("College 1 is older");
  else
    printf("College 2 is older");
	return 0;
}