#include <stdio.h>
int main()
{
  int x, y;
  scanf("%d", &x);
  scanf("%d", &y);
  if(x < y)
  {
    printf("College 1 is older\n");
  }
else
  
  {
     printf("College 2 is older\n");
  }
  
	return 0;
}