#include <stdio.h>
int main()
{
    int low=1,high=50,n,i=1,r1,r2;
    scanf("%d",&n);
    for(;i<n-1;i++)
    {
      r1=n%21;
    }   
      if((r1==0)&&(r2==0))
        printf("This balloon can fly to miney");
      else
        printf("This balloon cannot fly to miney");
          
    
  
	return 0;
}