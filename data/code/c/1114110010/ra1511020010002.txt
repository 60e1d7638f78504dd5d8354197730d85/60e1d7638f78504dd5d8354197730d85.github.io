#include <stdio.h>
int main()
{
	int n;
  	scanf("%d",&n);
  	if(n%21==0)
    {
      printf("This balloon can fly to miney");
    }
  	else
      printf("This balloon cannot fly to miney");
	return 0;
}