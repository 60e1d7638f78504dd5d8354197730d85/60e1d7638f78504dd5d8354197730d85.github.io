#include <stdio.h>
int main()
{int number;
 scanf("%d",&number);
 if(number%3==0&&number%7==0){
 printf("This balloon can fly to miney");
 }
 else{
   printf("This balloon cannot fly to miney");}

	return 0;
}