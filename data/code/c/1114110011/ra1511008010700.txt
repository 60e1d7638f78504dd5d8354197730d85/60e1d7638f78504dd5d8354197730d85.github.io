#include <stdio.h>
#include <math.h>
int main()
{
int number;
  scanf("%d",&number);
  if(number%2 == 0){
  
   if(number!=0 && number>=100)
  {
    printf("Invalid Number");
  }
    else 
      printf("Doll will sing");
  }
	return 0;
}