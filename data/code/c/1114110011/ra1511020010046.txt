#include <stdio.h>
int main()
{int num,even;
 scanf("%d",&num);
 if(num>0&&num<=100)
   if(num%2==0)
     printf("Doll will sing");
 else
   printf("Invalid Number");
 else
   printf("Invalid Number");

	return 0;
}