#include<stdio.h>
#include<stdlib.h>
int main()
{
  float a,b,c;
  scanf("%f",&a);
  scanf("%f",&b);
  c=(b-a);
  if(c<=0.5f)
  {
    printf("Approximate number");
  }
  else
  {
    printf("Not an Approximate number");
  }
	return 0;
}