#include <stdio.h>
int main()
{
  int s,r;
  scanf("%d",&r);
  scanf("%d",&s);
  
  if(r>18 && s>40)
  { printf("Eligible to donate");}
    else
       printf("Not Eligible to donate");
      


	return 0;
}