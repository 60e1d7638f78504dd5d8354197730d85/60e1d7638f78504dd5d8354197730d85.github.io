#include <stdio.h>
int main () {

   /* local variable definition */
   int a,b;
  scanf("%d %d",&b,&a);

   /* while loop execution */
   while( a <= b)
   {
      printf("%d\n", b);
      b--;
   }
 
   return 0;
}
