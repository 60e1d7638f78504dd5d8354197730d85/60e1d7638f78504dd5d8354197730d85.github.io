#include <stdio.h>
int main()
{
  int a;
  scanf("%d\n",&a);
  float b;
  scanf("%f\n",&b);
  int c;
  scanf("%d\n",&c);
  float d;
  
  switch(a)
  {
    case 1: if(a=1)
    {
      b=b*c;
      printf("Hotel Tariff\nRs.%.2f",b);
      break;
    }
    case 2: if(a=2)
    {
       b=b*c;
      printf("Hotel Tariff\nRs.%.2f",b);
      break;
    }
    case 3: if(a=3)
    {
       b=b*c;
      printf("Hotel Tariff\nRs.%.2f",b);
      break;
    }
    case 4: if(a=4)
    {
      b=b*c + b*c*20/100;
      printf("Hotel Tariff\nRs.%.2f",b);
      break;
    }
    case 5: if(a=5)
    {
      b=b*c + b*c*20/100;
      printf("Hotel Tariff\nRs.%.2f",b);
      break;
    }
    case 6: if(a=6)
    {
       b=b*c;
      printf("Hotel Tariff\nRs.%.2f",b);
      break;
    }
    case 7: if(a=7)
    {
       b=b*c;
      printf("Hotel Tariff\nRs.%.2f",b);
      break;
    }
    case 8: if(a=8)
    {
       b=b*c;
      printf("Hotel Tariff\nRs.%.2f",b);
      break;
    }
    case 9: if(a=9)
    {
       b=b*c;
      printf("Hotel Tariff\nRs.%.2f",b);
      break;
    }
    case 10: if(a=10)
    {
       b=b*c;
      printf("Hotel Tariff\nRs.%.2f",b);
      break;
    }
    case 11: if(a=11)
    {
       b=b*c;
      printf("Hotel Tariff\nRs.%.2f",b);
      break;
    }
    case 12: if(a=12)
    {
       b=b*c;
      printf("Hotel Tariff\nRs.%.2f",b);
      break;
    }
  }
	return 0;
}