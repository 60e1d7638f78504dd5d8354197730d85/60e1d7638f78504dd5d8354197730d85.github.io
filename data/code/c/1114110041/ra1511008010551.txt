#include <stdio.h>
int main()
{
  int n,reversedNum=0,remainder;
  scanf("%d",&n);
  
  while(n!=0)
  {
    remainder=n%10;
    reversedNum=reversedNum*10+remainder;
    n/=10;
  }
  printf("%d",reversedNum);
  
  

	return 0;
}