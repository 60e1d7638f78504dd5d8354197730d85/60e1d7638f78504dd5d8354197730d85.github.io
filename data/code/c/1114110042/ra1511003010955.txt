#include <stdio.h>
int main()
{
int n1,n2,lcm;
  scanf("%d",&n1);
  scanf("%d",&n2);
  lcm=(n1>n2)?n1:n2;
  while(1)
    {
    if (lcm%n1==0 && lcm%n2==0)
      {
      printf("%d",lcm);
      break;
    }
    ++lcm;
    }
	return 0;
}