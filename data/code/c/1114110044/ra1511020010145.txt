#include <stdio.h>
int main()
{
int i,j,n,a[50];
  scanf("%d",&n);
  for(i=0;i<n;i++)
    scanf("%d",&a[i]);
  for(i=0;i<n;i++)
  {
    for(j=i+1;j<n;j++)
    {
      if(a[j]==a[i])
        printf("%d ",a[i]);
    }
  }
	return 0;
}