#include <stdio.h>
int main()
{
	int n, i, a[100], t, p;
  	scanf("%d",&n);
  	for(i = 0; i < n; i++)
      scanf("%d",&a[i]);
  	scanf("%d%d",&t, &p);
  	for(i = n-1; i >= p - 1; i--)
      a[i+1] = a[i];
  	a[p - 1] = t;
  	for(i = 0; i <= n; i++)
      printf("%d ",a[i]);
	return 0;
}