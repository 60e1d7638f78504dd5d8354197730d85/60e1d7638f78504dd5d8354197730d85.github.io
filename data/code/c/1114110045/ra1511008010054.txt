#include <stdio.h>
int main()
{
  int n,i,a[10],ele,pos;
  scanf("%d",&n);
  for(i=0;i<n;i++)
  {
    scanf("%d",&a[i]);
  }
  scanf("%d",&ele);
  scanf("%d",&pos);
  for(i=n;i>=pos;i--)
  {
    a[i]=a[i-1];
  }
  a[pos-1]=ele;
  n++;
  for(i=0;i<n;i++)
  {
    printf("%d ",a[i]);
  }
	return 0;
}