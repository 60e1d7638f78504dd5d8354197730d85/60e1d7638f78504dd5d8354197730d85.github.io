#include <stdio.h>
int n,i,count=0;
int main()
{
scanf("%d",&n);
  //if(n == 100) printf("%d",)
  for(i=0;i<=n;i++){
    if(i=1 && i>n) count++;
    n=n/10;
    count++;}
    printf("%d",count);
	return 0;
}