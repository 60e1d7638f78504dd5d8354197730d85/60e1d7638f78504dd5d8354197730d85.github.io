#include <stdio.h>
int main()
{
int n,i,sum;
  scanf("%d",&n);
  for(i=1;i<=n;i++)
  {
    if (n%i==0)
    {
      sum+=i;
    }
  }
  if(n==(sum/2))
  {
    printf("%d is a perfect number",n);
  }
  else
  {
    printf("%d is not a perfect number",n);
  }
	return 0;
}