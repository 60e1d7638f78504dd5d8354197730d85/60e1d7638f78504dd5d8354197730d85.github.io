#include <stdio.h>
int main()
{
    int n,i,flag=0;
  scanf("%d",&n);
  for (i=0;i<=n;i++)
  {
       if (n==i*i)
       {
         printf("TRUE");
         flag=1;
         break;
       }
  }
  if(flag==0)
  {
    printf("FALSE");
  }
	return 0;
}