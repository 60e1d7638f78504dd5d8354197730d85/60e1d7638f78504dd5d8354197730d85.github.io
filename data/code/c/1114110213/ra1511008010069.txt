#include <stdio.h>
int main()
{
int i,n,a[10],k;
  scanf("%d",&n);
  for(i=0;i<n;i++)
    scanf("%d",&a[i]);
  scanf("%d",&k);
  for(i=0;i<n;i++)
  {
    if(a[i]==k)
      printf("%d",i);
  }
  
	return 0;
}