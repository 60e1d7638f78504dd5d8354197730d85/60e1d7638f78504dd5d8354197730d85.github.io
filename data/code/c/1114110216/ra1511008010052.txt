#include <stdio.h>
int getsum(int n)
{
 int sum=0;
  while(n!=0)
  {
    sum=sum+n%10;
    n=n/10;
  }
  return sum;
 }
int main()
{
  int t;
  scanf("%d",&t);
  while(t--)
  {
    int n;
    scanf("%d",&n);
    printf("%d",getsum(n));
   }
	return 0;
}