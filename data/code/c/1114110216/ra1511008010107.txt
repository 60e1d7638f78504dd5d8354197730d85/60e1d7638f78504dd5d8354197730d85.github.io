#include <stdio.h>
int main()
{
  int n,a[10],i,r,sum=0;
  scanf("%d",&n);
  for(i=0;i<n;i++)
  {
    scanf("%d",&a[i]);
  }
  for(i=0;i<n;i++)
  {
    while(a[i]!='\0')
    {
      r=a[i]%10;
      sum=sum+r;
      a[i]=a[i]/10;
    }
    printf("%d",sum);
  }
	return 0;
}