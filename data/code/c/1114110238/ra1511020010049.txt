#include <stdio.h>
int main()
{
  int n,a[100],i,sum=0;
  scanf("%d",&n);
  if(n==0)
    printf("0");
  else
  {
  for(i=0;i<n;i++)
  {
    scanf("%d",&a[i]);
  }
  for(i=0;i<n;i++)
  {
    if(a[i]<a[i+1])
  sum=sum+a[i];
  }
  }
  printf("%d",sum);
	return 0;
}