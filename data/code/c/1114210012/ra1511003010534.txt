#include <stdio.h>
#include<math.h>
int main()
{
	int n,i,j,a;
  	scanf("%d",&n);
  	for(i=1;i<=n;i++)
    {
      if(i%2==0)
        j=2;
      else
        j=1;
      a=pow(i,2)-j;
      printf("%d ",a);
    }
	return 0;
}