#include <stdio.h>
int main()
{
  	int n, i, j, val=1;
  	scanf("%d", &n);
  	for(i=0; i<n; i++){
		for(j=0; j<n; j++){
          	if(i>=j)
				printf("%d ", val++);
        }
      	printf("\n");
    }

	return 0;
}