#include <stdio.h>

int main()
{
    int i, j, k, N;

    scanf("%d", &N);

    k = 1;

    for(i=1; i<=N; i++)
    {
        for(j=1; j<=i; j++, k++)
        {
            printf("%d ", k);
        }

        printf("\n");
    }

    return 0;
}