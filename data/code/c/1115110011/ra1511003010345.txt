#include <stdio.h>
int main()
{
int n;
  scanf("%d",&n);
  int i,j,sum=n,a[100];
  for(i=0;i<n;i++)
  {
    scanf("%d",&a[i]);
    
  }
  for(i=0;i<n;i++)
  {
    for(j=i+1;j<n;j++)
    {
      if(a[i]==a[j])
        sum--;
    }
  }
  printf("%d",sum);
	return 0;
}