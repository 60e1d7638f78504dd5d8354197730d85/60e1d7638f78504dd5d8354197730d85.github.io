#include <stdio.h>
int main()
{
	int n;
  	scanf("%d",&n);
  	int a[n],i,s=0;
  	for(i=0;i<n;i++)
    {
      scanf("%d",&a[i]);
      if(a[i]>=0)
        s+=a[i];
    }
  printf("sum=%d",s);
	return 0;
}