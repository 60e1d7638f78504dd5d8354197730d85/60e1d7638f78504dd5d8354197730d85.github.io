#include <stdio.h>
int main()
{
  int n;
  scanf("%d",&n);
  int arr[n],i,sum;
  for(i=0;i<n;i++){
    scanf("%d",&arr[i]);
    if(arr[i]>0)
      sum+=arr[i];
  }
  printf("sum=%d",sum);

	return 0;
}