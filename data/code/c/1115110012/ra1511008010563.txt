#include <stdio.h>
int main()
{int x,i,sum=0;int arr[i];
 scanf("%d",&x);
 for(i=0;i<x;i++)
 {
   scanf("%d",&arr[i]);
 }
 for(i=0;i<x;i++)
 { if(arr[i]>0)
   sum+=arr[i];
   
 }
printf("sum=%d",sum);

	return 0;
}