#include <stdio.h>
int main()
{ int a[10],n,x,i,s=0;
 scanf("%d",&n);
       for(i=0;i<n;i++){
         scanf("%d",&x);
         if(x<0)
         { s=s+x;
         }}
       printf("sum=%d",s);
         

	return 0;
}