#include <stdio.h>
int main()
{
  int n,sum=0,i;
  scanf("%d",&n);
  int arr[n];
  for(i=0;i<n;i++)
  {
    scanf("%d",&arr[i]);
  }
  for(i=0;i<n;i++)
  {
    if(arr[i]<0)
    {
      int a=arr[i];
      sum=sum+a;
    }
  }
  printf("sum=%d",sum);
  
  
	return 0;
}