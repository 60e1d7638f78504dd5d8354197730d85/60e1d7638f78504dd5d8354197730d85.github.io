#include <stdio.h>
int main()
{
int n,i,sum=0;
  scanf("%d",&n);
  int arr[10];
  for(i=0;i<n;i++)
  {
  scanf("%d",&arr[i]);
    if(arr[i]<0){
    sum+=arr[i];}
  }
  printf("sum=%d",sum);
	return 0;
}