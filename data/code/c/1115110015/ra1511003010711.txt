#include <stdio.h>
int main()
{
  int n,a[100],i,sum=0,odd=0;
  scanf("%d",&n);
  for(i=0;i<n;i++){
    scanf("%d",&a[i]);}
  for(i=0;i<n;i++){
    if(a[i]%2==0){
      sum=sum+a[i];}
    else
      odd=odd+a[i];}
    printf("even=%d\n",sum);
    printf("odd=%d\n",odd);

	return 0;
}