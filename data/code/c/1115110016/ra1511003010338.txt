#include <stdio.h>
int main()
{

  int a[1000], i;
  int n,sum;
  sum = 0;
  scanf("%d", &n);
  for(i=0;i<n;i++) {
    scanf("%d", &a[i]);
  }
  
  for(i=0;i<n;i++) {
    if(a[i]%2!=0) {
      sum+=a[i];
    }
  }
    printf("odd=%d",sum); 
	return 0;
}