#include <stdio.h>
int main()
{
   int array[100], c, n;
  int sum=0;
    scanf("%d", &n);
  
   for (c = 0; c < n; c++)
      scanf("%d", &array[c]);
  
  for(c=0;c<n;c++){
  if(array[c]%2!=0)
    sum=sum+array[c];
    
  }
  printf("odd=%d",sum);
	return 0;
}