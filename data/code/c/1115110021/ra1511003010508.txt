#include <stdio.h>
int main()
{
int n,m;
  scanf("%d",&n);
  int a[n],i;
  for(i=0;i<n;i++)
  	scanf("%d",&a[i]);
  scanf("%d",&m);
  if(m<n)
  printf("%d",a[m]);
	else
      printf("Invalid Input");
  return 0;
}