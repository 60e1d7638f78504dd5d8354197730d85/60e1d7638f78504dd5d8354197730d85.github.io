#include <stdio.h>
int main()
{

  int arr[10];
  int n;
  int i,j,temp;
  int flag=0;
  scanf("%d",&n);
  for(i=0;i<n;i++)
  {
    scanf("%d",&arr[i]);
  }
  for(i=0;i<n-1;i++)
  {
    for(j=0;j<n-i-1;j++)
    {
      if(arr[j]>arr[j+1])
      {
        temp=arr[j];
        arr[j]=arr[j+1];
        arr[j+1]=temp;
      }
    }
  }

   int k= arr[n-1]-arr[0];
    printf("The range of the array is=%d",k);
	return 0;
}