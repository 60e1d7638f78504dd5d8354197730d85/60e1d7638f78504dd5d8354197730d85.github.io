#include <stdio.h>
int main()
{
  int n,i,j,a[10],b[10];
  scanf("%d",&n);
  for(i=0;i<n;i++)
    scanf("%d",&a[i]);
  for(i=0,j=n-1;i<n,j>=0;i++,j--)
  {
    b[i]=a[j];
  }
  for(i=0;i<n;i++)
    printf("%d ",b[i]);
  return 0;
}