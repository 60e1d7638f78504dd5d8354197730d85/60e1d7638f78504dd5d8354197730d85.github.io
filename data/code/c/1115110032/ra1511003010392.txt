#include <stdio.h>
int main()
{
int n,a[100],i,j,temp,b[100];
  scanf("%d",&n);
  for(i=0;i<n;i++)
  {
    scanf("%d",&a[i]);
  }
  for(i=0;i<n;i++)
  {
    for(j=i+1;j<n;j++)
    {
      if(a[j]==a[i])
      {
        a[j] = 0;
      }
    }
  }
  for(i=0;i<n;i++)
  {
    if(a[i] == 0)
      continue;
    else
      printf("%d ",a[i]);
  }
	return 0;
}