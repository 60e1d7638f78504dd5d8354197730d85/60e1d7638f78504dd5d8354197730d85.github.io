#include <stdio.h>
int main()
{
   int n;
  scanf("%d\n",&n);
  int a[n],i;
  for(i=0;i<n;i++)
  {
    scanf("%d ",&a[i]);
  }
  for(i=0;i<n;i++)
  {
    
      if(a[0]<a[i])
        a[0]=a[i];
  }
  printf("%d",a[0]);
  
  
	return 0;
}