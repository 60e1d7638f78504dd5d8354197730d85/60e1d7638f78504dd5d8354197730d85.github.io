#include <stdio.h>
int main()
{
  int i,n,a=0,b=0,num;
  scanf("%d",&n);
  for(i=0;i<n;i++)
  {
    scanf("%d",&num);
    if(num>a)
    {
      b=a;
      a=num;
    }
    else if(num>b)
      b=num;
  }
    printf("%d",b);

	return 0;
}