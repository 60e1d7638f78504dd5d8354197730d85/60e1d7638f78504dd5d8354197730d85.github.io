#include <stdio.h>
int main()
{
  int a[10],b[10],i,j,k,n;
  scanf("%d",&n);
  for(i=0;i<n;i++)
  {
    scanf("%d",&a[i]);
  }
  for(i=0;i<n-1;i++)
  {
    for(j=0;j<n-i-1;j++)
    {
      if(a[j+1]<a[j])
      {
        k=a[j+1];
        a[j+1]=a[j];
        a[j]=k;
      }
     
    }
  }
  for(i=0;i<n;i++)
  {
    printf("%d ",a[i]);
  }
  

	return 0;
}