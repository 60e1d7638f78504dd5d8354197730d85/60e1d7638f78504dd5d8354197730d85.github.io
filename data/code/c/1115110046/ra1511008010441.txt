#include <stdio.h>
int main()
{
int n,a[1000],i;
  scanf("%d",&n);
  printf("\n");
  for(i=0;i<n;i++)
  {
    scanf("%d",&a[i]);
  }
  for(i=0;i<n-1;i++)
  {
    printf("%d ",a[i]);
  }
  for(i=n-1;i<n;i++)
  {
    printf("%d",a[i]);
  }
  
  
	return 0;
}