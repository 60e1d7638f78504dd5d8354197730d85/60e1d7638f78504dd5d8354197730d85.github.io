#include <stdio.h>
int main()
{
  int n,odd=0,even=0,i;
  scanf("%d",&n);
  int arr[n];
  for(i=0;i<n;i++)
  {
    scanf("%d",&arr[i]);
  }
  for(i=0;i<n;i++)
  {
    if(arr[i]%2==0)
      even++;
    else
      odd++;
  }
  printf("%d\n%d",even,odd);

	return 0;
}