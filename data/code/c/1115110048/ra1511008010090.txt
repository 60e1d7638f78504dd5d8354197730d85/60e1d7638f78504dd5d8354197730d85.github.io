#include <stdio.h>
int main()
{
 int i,j,x,count=0,a[100];
  int n;
  scanf("%d",&n);
  for(i=0;i<n;i++)
  {
    scanf("%d",&a[i]);
  }
  scanf("%d",&x);
  for(i=0;i<n;i++)
  {
    if(a[i]==x)
      count++;
  }
  printf("%d",count);
	return 0;
}