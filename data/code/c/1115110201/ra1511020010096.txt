#include <stdio.h>
int main()
{
int n,j;
  scanf("%d",&n);
  int i,a[n];
  for (i=0;i<n;i++)
  {
    scanf("%d",&a[i]);
  }
  for (i=0;i<n;i++)
  {
    for(j=i+1;j<n;j++)
    {
      if(a[i]==a[j])
        a[j]=0;
    }
  }
  for (i=0;i<n;i++)
  {
    if(a[i]!=0)
      printf("%d ",a[i]);
  }
	return 0;
}