#include<stdio.h>
    #include<stdlib.h>
    int main()
    {
        int i,a; long int c,d,e,b;
        scanf("%d",&a);
        for(i=0;i<a;i++)
        {
            scanf("%ld %ld %ld %ld",&b,&c,&d,&e);
                if(b==0 || c==0 || d==0 ||e==0 || b+c==0 || b+d==0 || b+e==0 || c+d==0 || c+e==0 || d+e==0 ||b+c+d==0 || b+c+e==0 || b+d+e==0 || c+d+e==0 ||b+c+d+e==0 )
                   printf("Yes\n");
                 else
                    printf("No\n");
        }
        return 0;
}  