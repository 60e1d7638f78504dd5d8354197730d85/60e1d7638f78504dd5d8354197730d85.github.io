#include <stdio.h>
 
int main(void) {
	int n, i, j=0,x;
	    scanf("%d%d", &n,&x);
	    int a[n];
	    for(i=0; i<n; i++)
	        scanf("%d", &a[i]);
	    for(i=0; i<n; i++)
	    {
	        if(a[i]%x==0)
	            j++;
	       
	    }

	    printf("%d", j);
	return 0;
} 