#include <stdio.h>
int main()
{
 int n;
  scanf("%d",&n);
  int a[n];
  int b[n];
  int i,sum=0;
  for(i=1;i<=n;i++)
    scanf("%d",&a[i]);
  for(i=n;i>=1;i--)
  {
    sum*=-1;
    b[i]=a[i]-sum;
    sum+=b[i];
  }
  for(i=1;i<=n;i++)
    printf("%d ",b[i]);
  printf("\n");
	return 0;
}