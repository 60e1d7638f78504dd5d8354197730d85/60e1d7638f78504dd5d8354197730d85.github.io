#include <stdio.h>
int main()
{
	int n;
  scanf("%d", &n);
  int i, a[n];
  for(i=0;i<n;i++)
    scanf("%d",&a[i]);
  
  int b[n];
  
  b[n-1] = a[n-1];
  
  for(i=n-2;i>=0;i--)
    b[i] = a[i] + a[i+1];
  
  
  for(i=0;i<n;i++)
    printf("%d ",b[i]);
	return 0;
}