#include <stdio.h>

int main()
{
  int n,a[50],largest=-1,i;
  scanf("%d",&n);
  for(i=0;i<n;i++)
  {
    scanf("%d",&a[i]);
  }
  for(i=0;i<n;i++)
  {
    if(a[i]>largest)
    {
      largest=a[i];
    }
  }
  printf("%d",largest);
  return 0;
}