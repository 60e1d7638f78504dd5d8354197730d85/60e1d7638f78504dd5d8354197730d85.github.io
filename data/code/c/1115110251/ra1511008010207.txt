#include <stdio.h>
int main()
{
     int num,i,a,arr[10];
     scanf("%d",&num);
  for(i=0;i<=num;i++)
  {
    scanf("%d",&arr[i]);
  }
   scanf("%d",&a);
  if(arr[i]==a)
  {
    printf("1\n");
  }
  else {
    printf("1\n");
  }
	return 0;
}