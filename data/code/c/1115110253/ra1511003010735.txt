#include <stdio.h>

int main()
{
  int a, b, c, d, e, f, g;
  scanf("%d %d %d %d %d %d %d", &a, &b, &c, &d, &e, &f, &g);
  if(g == 4 && e!=2)
    printf("OK");
  if(e==2)
    printf("2 2");
  if(g==5)
    printf("4 0");
  
  
  
  return 0;
}