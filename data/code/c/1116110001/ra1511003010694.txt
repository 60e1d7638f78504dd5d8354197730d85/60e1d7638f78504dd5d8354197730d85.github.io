#include <stdio.h>
#include <string.h>
int main()
{
  char a[20];
  int i;
  scanf("%s",&a[i]);
  printf("The ASCII values of the string are:\n");
  for(i=0;a[i]!='\0';i++)
  printf("%d ",a[i]);
  
 

	return 0;
}