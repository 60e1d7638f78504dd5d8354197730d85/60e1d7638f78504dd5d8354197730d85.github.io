#include<stdio.h>
#include<string.h>
int main()
{
  char st[25],temp;
  int l,i,j;
  //clrscr();
 
  //printf("enter Any Sting\n");
  scanf("%s",st);
  l=strlen(st);
  /*Logic Bubble Sort  */
  for(i=1;i<l;i++)
     for(j=0;j<l-i;j++)
if(st[j]>st[j+1])
{
  temp=st[j];
  st[j]=st[j+1];
  st[j+1] =temp;
}
 // printf("sorted string  \n");
  printf("%s",st);
  return 0;
}