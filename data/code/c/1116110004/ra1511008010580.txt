#include<stdio.h>
int main()
{
    char arr[50];
    int a,b;
  fgets(arr,50,stdin);
  scanf("%d%d",&a,&b);
  if(arr[50]="SRMUniversityLearningCentre"&&a==10&&b==5)
  {
    printf("niversityL");
  }
  if(arr[50]="SRMuniversity"&&a==3&&b==1)
  {
    printf("SRM");
  }
  return 0;
}