#include<iostream>

#include <stdio.h>
#include<string.h>

using namespace std;
int main()
{
string x,y;
  getline(cin,x);
  getline(cin,y);
    
 string m=x+y;
  //printf("%s",x);
  cout<<x<<endl<<y<<endl<<m;
	return 0;
}