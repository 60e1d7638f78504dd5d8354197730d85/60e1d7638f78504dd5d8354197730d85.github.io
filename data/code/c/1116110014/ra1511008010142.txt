#include <stdio.h>
#include<string.h>
int main()
{
char str[100];
  int i;
  scanf("%s",str);
  for(i=0;str[i]!='\0';i++)
    if((str[i]>=65)&&(str[i]<=91))
      str[i]=(char)(str[i]+32);
  printf("%s",str);
  return 0;
}
