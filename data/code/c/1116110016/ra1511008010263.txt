#include <stdio.h>
#include <string.h>
int main()
{
char str[100];
  int i;
  scanf("%s",str);
  int len = strlen(str);
  for(i=0;i<len;i++){
    printf("%c",str[i]);
  }
 printf("\n");
  for(i=len-1;i>=0;i--){
    printf("%c",str[i]);
  }
	return 0;
}