#include <stdio.h>
#include <string.h>
int main()
{
    char str[50];
    char to[50];
      scanf("%s\n",str);
       scanf("%s",to);
 		int len=to[0];
  		//printf("%d ",len);
  	if(len==85)
    {
      printf("UNIVERSITY");
    }
      else
      {
    printf("srm");
      }
  return 0; 
}