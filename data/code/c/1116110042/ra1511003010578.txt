#include <stdio.h>
int main()
{
	char s[50];
  	int i=0,c=0;
  	scanf("%s",s);
  	while(s[i]!='\0'){
    	if(s[i]=='a' || s[i]=='e' || s[i]=='i' || s[i]=='o' || s[i]=='u'){
        	c++;
        }
      	i++;
    }
  	printf("%d",c);
	return 0;
}