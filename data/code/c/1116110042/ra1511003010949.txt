#include <stdio.h>
int main()
{
  char str[20];
  int i=0,count=0;
  scanf("%s",str);
  while(str[i]!='\0'){
  	switch(str[i]){
      case 'a':
      case 'e':
      case 'i':
      case 'o':
      case 'u':
        		count++;
        		break;
    }
    ++i;
  }
	printf("%d",count);
	return 0;
}