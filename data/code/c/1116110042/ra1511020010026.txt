#include <stdio.h>
int main()
{
	char word[20];
  	scanf("%s",word);
  int i,c;
  for(i=0;word[i]!='\0';i++)
  {
    if(word[i]=='a'||word[i]=='e'||word[i]=='i'||word[i]=='o'||word[i]=='u')
      c++;
  }
	printf("%d",c);
	return 0;
}