#include <stdio.h>
#include<string.h>
int main()
{
char a[100],i,n,flag;
  scanf("%s",a);
  n=strlen(a);
  for(i=0;i<n;i++)
  {
    if(a[i]=='a' ||a[i]=='e'||a[i]=='i'||a[i]=='o'||a[i]=='u')
      flag=1;
    else
      printf("%c",a[i]);
             }
  
	return 0;
}