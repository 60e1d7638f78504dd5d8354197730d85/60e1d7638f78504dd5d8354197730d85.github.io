#include <stdio.h>
#include <string.h>
int main()
{ char str[100];
  int n,i;
  scanf("%s",str);
  n=strlen(str);
  if (n>=10)
     printf("%s",str);
  else
     { printf("%s",str);
       for (i=n;i<10;i++)
           printf("*");
     }
	return 0;
}