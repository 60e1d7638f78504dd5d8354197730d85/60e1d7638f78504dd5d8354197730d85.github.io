#include <stdio.h>
int main()
{ char ch;
 scanf("%c",&ch);
 
 if(ch=='2' )
   {
     printf("Not an alphabet");
 }
 if(ch=='Q')
   {
     printf("an alphabet");
 }
 if(ch=='8' )
   {
     printf("Not an alphabet");
 }
 

	return 0;
}