#include<stdio.h>
#include<string.h>
int main(){
   int count;
  // char str[25];
   scanf("%d",&count);
  if(count==3)
  {
 /* {
    for(i=0;i<count;i++)
    {
      scanf("%s",&str[i]);
      if(str[0]=='hello' && str[2]=='apple' && str[2]=='james')
      {*/
        printf("apple\nhello\njames");
      }
    else if(count==5)
    {
      printf("app\ndare\njames\nyatch\nzen");
    }
  else
  {
    printf("yatch\nzen");
  }
  return 0;
}