#include <stdio.h>
int main()
{int a[100],n,i,s=0,k=0;
 scanf("%d",&n);
 for(i=0;i<n;i++){
   scanf("%d",&a[i]);
}
 for(i=0;i<n;i++)
 {
   if(a[i]%2==0)
   s++;
   else
   k++;
   }
 if(s>k)
   printf("READY FOR BATTLE");
 else 
   printf("NOT READY");
 

	return 0;
}