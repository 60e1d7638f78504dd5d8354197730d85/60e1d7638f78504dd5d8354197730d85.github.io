#include <stdio.h>
#include<stdlib.h>
int main()
{char c[50],a[50];
 int i,j,k;
 scanf("%s",&c[i]);
 for(i=0;c[i]!='\0';i++)
 {c[i]=tolower(c[i]);
 }
for(i=0;c[i]!='\0';i++){
 printf("%c",c[i]);
 }

	return 0;
}