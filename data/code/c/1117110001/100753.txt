#include <stdio.h>
int main()
{  
  int a[10],max,i,n;
  scanf("%d",&n);
  for(i=0;i<n;i++)
    scanf("%d",&a[i]);
   max=a[0];
   for(i=1;i<n;i++)
   {
     if(max<a[i])
       max=a[i];
   }
  printf("%d is the maximum element in the array",max);
  
  
  

	return 0;
}