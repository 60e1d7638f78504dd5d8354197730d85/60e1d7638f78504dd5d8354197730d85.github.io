#include<stdio.h>
int main(){
int n,i,arr[100],sum=0;
scanf("%d",&n);
for(i=0;i<n;i++)
{
	scanf("%d",&arr[i]);
	if(arr[i]>sum)
	{
		sum=arr[i];
	}
}

printf("%d is the maximum element in the array",sum );
	return 0;
}