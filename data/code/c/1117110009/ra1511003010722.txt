#include <stdio.h>
int main()
{
int n,i,sum=0,sum1=0;
  scanf("%d",&n);
  int a[n];
  for(i=0;i<n;i++)
    scanf("%d",&a[i]);
  for(i=0;i<n;i++)
  {
    if(a[i]%2==0)
      sum+=a[i];
    else
      sum1+=a[i];
  }
  printf("even=%d\n",sum);
  printf("odd=%d\n",sum1);
	return 0;
}