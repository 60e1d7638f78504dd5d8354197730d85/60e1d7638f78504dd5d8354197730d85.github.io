#include <stdio.h>
int main()
{
	int a[20][20],i,j,m,n,s=0;
  	scanf("%d%d",&m,&n);
  	for(i=0;i<m;i++)
      for(j=0;j<n;j++)
      {
        scanf("%d",&a[i][j]);
        s+=a[i][j];
      }
  printf("sum=%d",s);
	return 0;
}