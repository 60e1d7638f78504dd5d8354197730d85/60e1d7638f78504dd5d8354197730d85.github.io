#include <stdio.h>
int main()
{
  int r,c, i, j, t;
  scanf("%d %d", &r, &c);
  for (i = 0; i < r; i++ )
    	for (j = 0; j < c; j++) {
          scanf("%d", &t);
          if (i < j && t) {
            puts("no");
            return 0;
          }
        }
  puts("yes");
            
          	

	return 0;
}