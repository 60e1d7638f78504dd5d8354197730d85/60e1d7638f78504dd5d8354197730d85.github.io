#include <stdio.h>
int main()
{
  int n,i,arr[100],max=0;
  scanf("%d",&n);
  for(i=0;i<n;i++)
  {
	scanf("%d",&arr[i]);  
  }
  for(i=0;i<n;i++)
  {
    if(arr[i]>max)
    {
      max=arr[i];
    }
  }
  printf("Maximum element in the array is %d",max);
  

	return 0;
}