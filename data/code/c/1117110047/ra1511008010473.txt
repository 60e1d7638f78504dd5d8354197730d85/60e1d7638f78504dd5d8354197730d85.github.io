#include <stdio.h>
int Two(int n)
{
  if (n == 0)
    return 0;
  while (n != 1)
  {
    if (n%2 != 0)
      return 0;
    n = n/2;
  }
  return 1;
}
int main()
{

  int n,f;
  scanf("%d",&n);
  f=Two(n);
  if(f==1)
    printf("YES");
  else
    printf("NO");
	return 0;
}