#include <stdio.h>
int main()
{
  int count,i,j,n,a[100];
  count=0;
  scanf("%d",&n);
  for(i=0;i<n;i++)
    scanf("%d",&a[i]);
  for(i=0;i<n;i++){
    for(j=0;j<n;j++){
   		if(i<j){
          if(a[i]>a[j]){
            count++;}}
    }
  }
    printf("%d",count);
  

	return 0;
}