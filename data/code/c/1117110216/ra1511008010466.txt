#include <stdio.h>
int main()
{
  int n,a[100],i,j; int c=0;
  scanf("%d",&n);
  for(i=0;i<n;i++)
    scanf("%d ",&a[i]);
  for(i=0;i<n;i++)
  {
    for(j=0;j<n;j++)
    {
      if((i<j) && (a[i]>a[j]))
        c++;
    }
  }
  printf("%d",c);
      

	return 0;
}