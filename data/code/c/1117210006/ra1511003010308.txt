#include<stdio.h>
int main()
{
   long  int t,a[1000022],i,j,k,n,x,y,z,max,count;
    scanf("%ld",&t);
    while(t--)
    {
        scanf("%ld",&n);
        j=0;
        max=-1;
        count=0;
        for(i=1;i<=1000010;i++)
        a[i]=0;
        for(i=0;i<n;i++)
        {
            scanf("%ld%ld",&x,&y);
            if(y==0)
            count=0;
            else 
            {
                if(a[x]==1)
                continue;
                if(y==1)
                {
                    count++;
                    a[x]=1;
                    if(max<count)
                    max=count;
                }
            }
        }
        if(max<count)
        max=count;
        printf("%ld\n",max);
    }
}