#include <stdio.h>
int main()
{
  int a,b;
  scanf("%d%d",&a,&b);
  printf("G.C.D of %d and %d is %d.",a,b,gcd(a,b));
   return 0;
}
  int gcd(int a,int b)
         {
           if(b!=0)
             gcd(b,a%b);
           else
             return a;
         }
             
         