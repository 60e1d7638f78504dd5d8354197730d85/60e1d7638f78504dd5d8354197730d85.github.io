#include <stdio.h>
int gcd(int n1,int n2)
{
  if(n2!=0)
  	return gcd(n2,n1%n2);
  else
    return n1;
}
int main()
{
	int x,y,g;
  	scanf("%d%d",&x,&y);
  	g=gcd(x,y);
  	printf("G.C.D of %d and %d is %d.",x,y,g);
	return 0;
}