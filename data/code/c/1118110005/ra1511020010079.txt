#include <stdio.h>
struct Year
{
  int yr;
}year;
int main()
{  int y;
  scanf("%d",&y);
  if(y%4==0 )
    printf("%d is a leap year",y);
  else
    printf("%d is not a leap year",y);
     return 0;
}