#include <stdio.h>
struct date
{
  int year;
}k;
void isleapyear( int y)
{
  if(y%4==0)
 {
   printf("%d is a leap year",y);
 }
 else
   printf("%d is not a leap year",y);
}
int main()
{ int y;
 scanf("%d",&k.year);
 void isleapyear(int);
 isleapyear(k.year);

	return 0;
}