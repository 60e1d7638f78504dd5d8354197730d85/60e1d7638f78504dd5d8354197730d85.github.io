#include <stdio.h>
struct JAM{
  int a;
  int b;
}jam;
int main()
{
int a,b,c,d,e,f;
  scanf("%d %d",&a,&b);
  scanf("%d %d",&c,&d);
  e=a/b;
  f=c/d;
  if(e>f)
    printf("%d/%d is greater than %d/%d",a,b,c,d);
    else
      printf("%d/%d is smaller than %d/%d",a,b,c,d);
	return 0;
}