#include <stdio.h>
#include<math.h>
struct money
{
  int s;
}e;
int main()
{
   int n,i;
   int x=0;
   scanf("%d ",&e.s);
   scanf("%d",&n);
    for(i=1;i<=n;i++)
    {
      x=e.s*pow(2,i);
    }
  printf("%d",x);
	return 0;
}