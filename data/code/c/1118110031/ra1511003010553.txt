#include<string.h>
#include <stdio.h>
struct no
{
  char name[50];
  
};
int main()
{
  int i;
  struct no s;
  char str[50];
 int num=0;
  scanf("%s",s.name);

  printf("%zd",strlen(s.name));

  

	return 0;
}