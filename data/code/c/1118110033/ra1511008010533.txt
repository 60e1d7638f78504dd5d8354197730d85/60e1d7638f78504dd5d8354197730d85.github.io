#include <stdio.h>
#include<string.h>
struct name
{
  char ename[50];
};

int main()
{
 struct name n1;
 int s;
  int counter=0,c=0;
  scanf("%s",n1.ename);

s=strlen(n1.ename);
 printf("%d",s); 
	return 0;
}