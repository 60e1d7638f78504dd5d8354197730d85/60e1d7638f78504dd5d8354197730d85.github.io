#include <stdio.h>
#include<string.h>
struct student
{
    char name[50];
  } s;

int main()
{

 int count_char;
// char str[20];

scanf("%s",s.name);
count_char=strlen(s.name);
 printf("\n%d",count_char);



	return 0;
}