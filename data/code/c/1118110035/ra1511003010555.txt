#include <stdio.h>
#include<string.h>
struct friends{
	char name[100];
	int n;
	};
	struct friends f1;
int main()
{	
  int i,d,c=0;
 
  scanf("%s",f1.name);
   d=strlen(f1.name);
  
  for(i=0;i<d;i++)
  { c++;
  }
  printf("%d",c);
  
	  
	return 0;
}