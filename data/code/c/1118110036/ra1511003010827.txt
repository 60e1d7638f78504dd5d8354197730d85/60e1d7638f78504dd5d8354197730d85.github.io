#include <stdio.h>
struct stud
{
    int  s1, s2, s3 ;
} s[3] ;
int main()
{
  int i,n=3;

        //printf("\nEnter the roll number : ") ;
        scanf("%d %d %d", &s[i].s1, &s[i].s2, &s[i].s3) ;

 // for(i = 0 ; i < n ; i++)
   // {
        printf("%d %d %d  \n",s[i].s1,s[i].s2,s[i].s3);
  //}
	return 0;
}