#include <stdio.h>
int main()
{
    int a,b,c,sum,avg;
    scanf("%d",&a);
    scanf("%d",&b);
    scanf("%d",&c);
    sum=a+b;
    avg=(a+b+c)/3;
    printf("%d %d %d %d",a,b,c,avg);
    return 0;
}
