#include <stdio.h>
#include <string.h>
  struct total{
  char a[20];
    int x,y,z;
  }t;
int main()
{char s[30];
 int a,b,c;
scanf("%s",s);
scanf("%d",&a);
 scanf("%d",&b);
 scanf("%d",&c);
 printf("%s ",s);
 printf("%d ",a+b+c);
	return 0;
}