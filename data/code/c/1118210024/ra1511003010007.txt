#include <stdio.h>
struct arr
{
  int sum;
};
int main()
{
  struct arr p;
int i,a[10];
  p.sum=0;
  for(i=0;i<5;i++)
    scanf("%d",&a[i]);
  for(i=0;i<5;i++)
    p.sum+=a[i];
  printf("%d",p.sum);
	return 0;
}