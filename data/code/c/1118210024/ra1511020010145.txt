#include <stdio.h>
int main()
{
int n,i,sum=0;
  //scanf("%d",&n);
  int a[5];
  for(i=0;i<5;i++)
    scanf("%d",&a[i]);
  for(i=0;i<5;i++)
  {
 
      sum=sum+a[i];
  }
  printf("%d",sum);
	return 0;
}