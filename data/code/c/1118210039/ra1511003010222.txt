#include <stdio.h>
int main()
{
	int j, a[100], i;
	for(i = 0; i < 10; i++)
      scanf("%d",&a[i]);
  	for(i = 0; i < 10; i++)
      for(j = 0; j < 9 - i; j++)
        if(a[j] > a[j+1])
        {
          int t = a[j];
          a[j] = a[j+1];
          a[j+1] = t;
        }
  	for(i = 0; i < 10; i++)
      if(a[i]%2==0)
        printf("%d ",a[i]);
	return 0;
}