#include <stdio.h>
int main()
{
  int a,b,sum=0;
  scanf("%d%d",&a,&b);
  sum=a+b;
  printf("\nThe sum of the numbers is %d",sum);
  printf("\nAccessing a string using pointer");
  printf("\nHello");

	return 0;
}