#include <stdio.h>
int main()
{
int num1,num2,total;
  scanf("%d",&num1);
   scanf("%d",&num2);
  total=num1+num2;
  printf("The sum of the numbers is %d",total);
  printf("\nAccessing a string using pointer");
  printf("\nHello");
	return 0;
}