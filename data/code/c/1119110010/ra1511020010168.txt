#include <stdio.h>
int main()
{
int first,second,*p,*q,sum;
  scanf("%d",&first);
  scanf("%d",&second);
  p=&first;
  q=&second;
  sum=*p+*q;
  printf("The sum of the numbers is %d\n",sum);
  printf("Accessing a string using pointer\n");
  printf("Hello\n");
	return 0;
}