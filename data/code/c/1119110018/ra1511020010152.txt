#include <stdio.h>

int main()
{
    char ch;
    scanf("%c", &ch);
    if(ch >= 'A' && ch <= 'Z')
    {
        printf("Input is upper case\nLower case=%c", ch+32);
    }
    else if(ch >= 'a' && ch <= 'z')
    {
        printf("Input is lower case\nUpper case=%c", ch-32);
    }
    else
    {
        printf("Input is lower case\nUpper case=%c", ch);
    }

    return 0;
}