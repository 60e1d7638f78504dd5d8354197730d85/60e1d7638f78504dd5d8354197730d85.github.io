#include <stdio.h>
#include <string.h>
int main()
{
char s[1000],*ptr;
  int len,i;
  scanf("%[^\n]s",s);
  ptr=s;
  for(i=0;i<1000;i++)
  {
    if(*ptr=='\0')
      break;
    ptr++;
  }
  len=i;
  ptr--;
  for(i=len;i>0;i--)
  {
    printf("%c",*ptr--);
  }
	return 0;
}