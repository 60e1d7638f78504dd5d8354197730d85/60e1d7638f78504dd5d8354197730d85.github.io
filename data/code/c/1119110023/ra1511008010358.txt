#include <stdio.h>
int main()
{
	int n;
  	scanf("%d", &n);
  	int arr[n],i;
  	for(i=0;i<n;i++)
      	scanf("%d", &arr[i]);
  	for(i=0;i<n;i++)
      	printf("\nelement %d=%d", i,arr[i]);
	return 0;
}