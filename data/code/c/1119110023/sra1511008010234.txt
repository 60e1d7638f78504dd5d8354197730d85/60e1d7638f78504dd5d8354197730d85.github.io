#include <stdio.h>
int main()
{
	int n,i,j;
  	scanf("%d",&n);
  	int ar[n];
  	for(i=0;i<n;i++)
    {
      scanf("%d",&ar[i]);
    }
  	for(j=0;j<n;j++)
    {
      printf("element %d=%d\n",j,ar[j]);
    }
	return 0;
}