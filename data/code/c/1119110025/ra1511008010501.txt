#include <stdio.h>
int main()
{
	int n,m;
  scanf("%d %d",&n,&m);
  	if(n>m)
    {
      printf("%d is the maximum number",n);
    }
  else 
  {
    printf("%d is the maximum number",m);
  }
	return 0;
}