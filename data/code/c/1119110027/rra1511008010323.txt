#include<stdio.h>
int main()
{
    int num1, num2;
    scanf("%d%d", &num1, &num2);

    if (num1 > num2)
    {
        printf("The number %d is larger", num1);
    }
    else
    {
        printf("Largest number is %d.", num2);
    }

    return 0;
}