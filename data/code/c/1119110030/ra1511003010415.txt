#include <stdio.h>
#include <string.h>
int main()
{
  char s[100];
  int len,i;
  scanf("%s",s);
  len=strlen(s);
  printf("Reverse of the string is:");
  for(i=len;i>-1;i--)
    printf("%c",s[i]);

	return 0;
}