#include <stdio.h>
#include <string.h>
int main()
{ int i=0;
  char str[100],*p;
  scanf("%s",str);
 int l=strlen(str);
 p=&str[l-1];
  for(i=0;str[i]!='\0';i++)
  {
    printf("%c",*p);
    p--;
  }

	return 0;
}