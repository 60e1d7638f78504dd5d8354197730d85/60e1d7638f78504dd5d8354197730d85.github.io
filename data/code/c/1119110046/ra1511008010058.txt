#include <stdio.h>
#include<string.h>
int main()
{
	char a[100];
	int len,i;
	//printf("\nENTER A STRING: ");
	scanf("%s",a);
	len=strlen(a);
	//printf("\nTHE REVERSE OF THE STRING IS:");
	for(i=len;i>=0;i--)
		printf("%c",(a[i]));
	return 0;
}