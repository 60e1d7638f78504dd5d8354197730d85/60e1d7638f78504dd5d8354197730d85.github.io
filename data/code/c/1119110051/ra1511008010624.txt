#include <stdio.h>
 

void swap(int *xp, int *yp)
{
    int temp = *xp;
    *xp = *yp;
    *yp = temp;
}
 
int main()
{
    int x, y;
   
    scanf("%d", &x);
    
    scanf("%d", &y);
    swap(&x, &y);
    printf("\n%d %d", x, y);
    return 0;
}