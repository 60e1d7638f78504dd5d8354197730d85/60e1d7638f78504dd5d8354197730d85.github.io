#include <stdio.h>
int main()
{
  int i,sum=0,c[7];
  int *ptr;
  for(i=0;i<6;i++)
  {
    scanf("%d",&c[i]);
  }
  ptr=c;
  for(i=0;i<6;i++)
  {
    sum=sum+*ptr;
    ptr++;
  }
  printf("%d",sum);
    

	return 0;
}