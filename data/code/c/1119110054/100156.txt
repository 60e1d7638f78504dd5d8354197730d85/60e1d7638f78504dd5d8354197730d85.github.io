#include <stdio.h>
int main()
{  int v1,v2;
  // int *ptr,*ptr1;
 	scanf("%d",&v1);
    scanf("%d",&v2);
    // *ptr = v1;
   //  *ptr1 = v2;
    if(v1 > v2) { printf("%d",v1);}
    else printf("%d",v2);
    
	return 0;
}