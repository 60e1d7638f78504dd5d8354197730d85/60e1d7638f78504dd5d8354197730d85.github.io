#include <stdio.h>
#include<string.h>
int main()
{
  char a[100],b;
  int i;
  scanf("%[^\n]\n%c",a,&b);
  for(i=0;i<strlen(a);i++)
  {
    if(a[i]!=b)
    {
      printf("%c",a[i]);
    }
  }

	return 0;
}