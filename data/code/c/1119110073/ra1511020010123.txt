#include<stdio.h>
#include<string.h>
 
void del(char str[], char ch);
 
int main() {
   char str[100],z[100];
   char ch;

   scanf("%s",str);
   scanf("%s",z);
   scanf("%s",&ch);
 
   int i, j = 0;
   int size;
   char ch1;
   char str1[100];
   size = strlen(str);
   for (i = 0; i<size; i++) {
      if (str[i] != ch) {
          ch1=str[i];
         str1[j]=ch1;
         j++;
      }
   }
  str1[j] = '\0';
 
   printf("%s University",str1);
  return 0;
}