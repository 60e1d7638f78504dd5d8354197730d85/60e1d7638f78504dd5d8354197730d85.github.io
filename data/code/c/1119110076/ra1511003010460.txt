#include <stdio.h>
int main()
{
    int n,i;
    scanf("%d",&n);
    int arr[n];
    for(i=0;i<n;i++)
    {
      scanf("%d",&arr[i]);
    }
   n--;
    while(n>=0)
    {
      printf("%d\n",arr[n]);
      n--;
    }
	return 0;
}