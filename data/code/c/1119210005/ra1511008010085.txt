#include <stdio.h>
 
int main()
{
    int i, num, odd_sum = 0, even_sum = 0;
    int a[10];
    scanf("%d", &num);
    for (i = 1; i <= num; i++)
      scanf("%d",&a[i]);
    for (i = 1; i <= num; i++)
        if (a[i] % 2 == 0)
            even_sum = even_sum + a[i];
    printf("sum=%d", even_sum);
  return 0;
}