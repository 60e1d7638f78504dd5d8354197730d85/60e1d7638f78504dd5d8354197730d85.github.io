#include <stdio.h>
#include<stdlib.h>
int main()
{
int n,i,even=0,odd=0;
  scanf("%d",&n);
  int arr[n];
  for(i=0;i<n;i++)
  {
scanf("%d",&arr[i]);
    if(arr[i]%2==0) even=even+arr[i];
    else odd=odd+arr[i];
  }
  printf("evensum=%d\n",abs(even));
  printf("oddsum=%d",abs(odd));
	return 0;
}