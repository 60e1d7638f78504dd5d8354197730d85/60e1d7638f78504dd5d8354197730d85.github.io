#include <stdio.h>
int main()
{
  int n,i,a[1000],j,cnt=0;
  scanf("%d",&n);
  for(i=0;i<n;i++){
    scanf("%d",&a[i]);
  }
  for(i=0;i<n;i++){
    for(j=i+1;j<n;j++){
        if(a[j]<a[i])
          cnt++;
    }
  }
  printf("%d",cnt);

	return 0;
}