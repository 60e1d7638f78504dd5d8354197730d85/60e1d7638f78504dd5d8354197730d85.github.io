#include <stdio.h>
int main()
{
  int n;
  scanf("%d",&n);
  int a[n],i,sum=0;
  for(i=0;i<n;i++)
  {
    scanf("%d",&a[i]);
    sum+=a[i];
  }
  if(n==0)
	printf("0");
  else
    printf("Mean:%.2f",(float)sum/n);
	return 0;
}