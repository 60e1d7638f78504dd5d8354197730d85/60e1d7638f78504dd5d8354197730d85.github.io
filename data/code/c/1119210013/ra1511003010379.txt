#include <stdio.h>
int main()
{
  int n,i;
  scanf("%d",&n);
  int a[n];
  for(i=0;i<n;i++)
    scanf("%d",&a[i]);
  
  int m=a[0];
  int mi=a[0];
  for(i=0;i<n;i++)
  {
    if(a[i]>m)m=a[i];
    
    if(a[i]<mi)mi=a[i];
  }
  printf("%d",m-mi);
  
    

	return 0;
}