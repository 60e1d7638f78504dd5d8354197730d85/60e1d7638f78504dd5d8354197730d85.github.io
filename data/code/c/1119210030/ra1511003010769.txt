#include <stdio.h>
int main()
{
	int t,a,b,c,i;
  scanf("%d",&t);
  	for(i=0;i<t;i++){
    scanf("%d %d %d",&a,&b,&c);
      if(a>25 && a<180 && b>=40 && b<180 && c>40 && c<180){
     if(a+b+c==180)
       printf("YES\n");
      else
        printf("NO\n");
    }
        else
    printf("NO\n");
    }

	return 0;
}