#include <stdio.h>
int main()
{
 int n,a,b,c,i;
  scanf("%d",&n);
  for(i=1;i<=n;i++){
    scanf("%d %d %d",&a,&b,&c);
  if(a>0 &&b>0 &&c>0 && a+b+c==180)
    printf("YES\n");
  else
    printf("NO");
  }
	return 0;
}