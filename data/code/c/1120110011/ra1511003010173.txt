#include <stdio.h>
int main()
{
  int a,b,l,d;
  scanf("%d%d",&a,&b);
  l=2*(a+b);
  printf("Required length is %d m",l);
  d=a*b;
  printf("\nRequired quantity of carpet is %d sqm",d);
  

	return 0;
}