#include <stdio.h>
int main()
{
int l,b,p,a;
 scanf("%d",&l);
 scanf("%d",&b);
  p=2*(l+b);
  a=l*b;
printf("Required length is %d m\n",p);
printf("Required quantity of carpet is %d sqm",a);
	return 0;
}