#include <stdio.h>
int main()
{
int len,wid;
  //p,a;
  scanf("%d",&len);
  scanf("%d",&wid);
  //p=(2*(l+w));
  //a=();
    printf("Required length is %d m\n",2*len+2*wid);
  	printf("Required quantity of carpet is %d sqm\n",len*wid);
	return 0;
}