#include <stdio.h>
#include <math.h>
int main()
{
	float a,b,diff;
  	scanf("%f %f",&a,&b);
  	diff = a-b;
  	diff= diff>0?diff:-1*diff;
  	if(diff<=0.5){
    	printf("Approximate number");
    }else printf("Not an Approximate number");
	return 0;
}