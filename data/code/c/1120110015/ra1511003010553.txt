#include <stdio.h>
int main()
  
{
  int n,i,a[50],s=0;
  scanf("%d",&n);
  for(i=0;i<n;i++)
    scanf("%d",&a[i]);
  for(i=0;i<n;i++)
  {
    if(a[i]<0)
      s=s+a[i];
  }
  printf("sum=%d",s);
  

	return 0;
}