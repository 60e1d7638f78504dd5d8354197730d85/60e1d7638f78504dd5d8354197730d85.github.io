#include <stdio.h>
int main()
{
 int i,n,a[100],neg=0;
  scanf("%d",&n);
  for(i=1;i<=n;i++){
    scanf("%d",&a[i]);
  }
  for(i=1;i<=n;i++)
  if(a[i]<0)
    neg=neg+a[i];
  
  printf("sum=%d",neg);
    
  
	return 0;
}