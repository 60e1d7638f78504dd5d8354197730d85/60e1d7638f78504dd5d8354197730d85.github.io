#include <stdio.h>
int main()
{ int n,i,sum=0;
 scanf("%d",&n);
 int num[n];
 for(i=0;i<n;i++)
 { scanf("%d",&num[i]);}
 for(i=0;i<n;i++)
 { if(num[i]<=0)
   sum+=num[i];
 }
 printf("sum=%d",sum);
	return 0;
}