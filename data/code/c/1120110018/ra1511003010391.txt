#include <stdio.h>
#include<math.h>
int main()
{ 
    char str[500];
    //unsigned int a;
    int i;
    scanf("%s",str);
    for(i=0;str[i]!='\0';i++)
    {
        //a=str[i];
       
          printf("%d ",str[i]);
      
    }
	return 0;
}