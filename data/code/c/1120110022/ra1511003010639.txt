#include <stdio.h>
int main()
{
  int i,j,n;
  scanf("%d",&n);
  int a[n],count=0;
  for(i=0;i<n;i++)
    scanf("%d",&a[i]);
  for(i=0;i<n;i++){
    for(j=0;j<n;j++){
      if((a[i]>a[j])&&(i<j)){
        count++;
      }
    }
  }
	printf("%d",count);
	return 0;
}