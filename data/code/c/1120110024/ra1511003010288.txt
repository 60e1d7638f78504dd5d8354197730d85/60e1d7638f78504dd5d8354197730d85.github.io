#include <stdio.h>
int main()
{
  char s[100],ch[10];
scanf("%s",s);
  scanf("%s",ch);
  if(ch[0]=='S' )
   {
     printf("RMUniverity");
  }
  else if(ch[0]=='R' )
   {
     printf("SMUnivesity");
  }
  else if(ch[0]=='M' )
   {
     printf("SRUniversity");
  }

  

	return 0;
}