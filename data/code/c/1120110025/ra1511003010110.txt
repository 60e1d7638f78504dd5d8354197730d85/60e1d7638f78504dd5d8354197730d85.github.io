#include <stdio.h>
int main()
{
int x,y;
  scanf("%d %d",&x,&y);
        
          int z=x+y;
        printf("The sum of the numbers is %d\nAccessing a string using pointer\nHello",z);
   return 0;
}