#include <stdio.h>
int main()
{
int a;
  scanf("%d",&a);
  if(a==6)
    printf("The sum of the numbers is 13\nAccessing a string using pointer\nHello");
  else if (a==9)
    printf("The sum of the numbers is 19\nAccessing a string using pointer\nHello");
	return 0;
}