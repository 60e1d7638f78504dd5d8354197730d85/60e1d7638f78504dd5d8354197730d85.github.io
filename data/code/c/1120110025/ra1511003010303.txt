#include <stdio.h>
int main()
{
	int x,y;
  scanf("%d%d",&x,&y);
  sum(x,y);
	return 0;
}
int sum(int a, int b){
	
  printf("The sum of the numbers is %d",a+b);
  printf("\nAccessing a string using pointer");
  printf("\nHello");
  return 0;
}