#include <stdio.h>
int main()
{
int a, b, sum;
  scanf("%d",&a);
  scanf("%d",&b);
  sum = a + b;
  printf("The sum of the numbers is %d",sum);
  printf("\nAccessing a string using pointer");
  printf("\nHello");
	return 0;
}