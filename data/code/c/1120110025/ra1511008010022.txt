#include <stdio.h>
int main()
{
int a,b,result;
  char ch;
  scanf("%d%d",&a,&b);
  result=a+b;
  printf("The sum of the numbers is %d",result);
  printf("\nAccessing a string using pointer");
  printf("\nHello");
  
	return 0;
}