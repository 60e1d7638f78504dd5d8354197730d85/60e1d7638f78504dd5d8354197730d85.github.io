#include <stdio.h>
int main()
{
int a,b,c;
  scanf("%d%d",&a,&b);
  c=a+b;
  printf("The sum of the numbers is %d",c);
  printf("\nAccessing a string using pointer");
  printf("\nHello");
	return 0;
}