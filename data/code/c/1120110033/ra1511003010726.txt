#include <stdio.h>
int main()
{
  int a[1000];
  int n,i;
  scanf("%d",&n);
  for(i=0;i<n;i++)
  {
   a[i]=0; 
  }
  for(i=0;i<n;i++){
  scanf("%d",&a[i]);
  }
  for(i=n-1;i>=0;i--){
   printf("%d ",a[i]); 
  }

	return 0;
}