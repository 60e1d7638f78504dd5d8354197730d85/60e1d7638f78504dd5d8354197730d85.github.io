#include <stdio.h>
int main()
{
    int n,i,max=0,min=100000;
    scanf("%d",&n);
    int arr[n];
    for(i=0;i<n;i++)
    {
      scanf("%d",&arr[i]);
      if(max<arr[i])
      {max=arr[i];}
      if(min>arr[i])
      {min=arr[i];}
    }
   printf("%d\n%d",max,min);
	return 0;
}