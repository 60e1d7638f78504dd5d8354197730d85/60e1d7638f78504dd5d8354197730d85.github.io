#include <stdio.h>
int main()
{
  int n;
  scanf("%d",&n);
  int i,a[100];
  for(i=0;i<n;i++)
  {
    scanf("%d",&a[i]);
  }
  int s=a[0];
  int l=a[0];
  for(i=1;i<n;i++)
  {
    if(s>a[i])
    {
      s=a[i];
    }
    if(l<a[i])
    {
      l=a[i];
    }
  }
  printf("%d\n%d",l,s);
	return 0;
}