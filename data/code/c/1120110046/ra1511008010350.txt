#include <stdio.h>
int main()
{

  int n,a,b,c,d,e;
  scanf("%d",&n);
  a=n*1;
  b=n*2;
  c=n*3;
  d=n*4;
  e=n*5;
  printf("1 * %d = %d",n,a);
  printf("\n2 * %d = %d",n,b);
  printf("\n3 * %d = %d",n,c);
  printf("\n4 * %d = %d",n,d);
  printf("\n5 * %d = %d",n,e);

	return 0;
}