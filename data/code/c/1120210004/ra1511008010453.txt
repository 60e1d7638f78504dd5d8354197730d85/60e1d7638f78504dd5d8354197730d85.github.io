#include <stdio.h>
int main()
{
char a[100], b[100],i;
 
   //printf("Enter a string\n");
   scanf("%s",&a[i]);
 
   //printf("Enter a string\n");
  scanf("%s",&b[i]);
 
   if (strcmp(a,b) == 0)
      printf("It is correct");
   else
      printf("It is wrong");
	return 0;
}