#include <stdio.h>
#include <string.h>
int main()
{
 char str[50];
  int length;
  scanf("%s",str);
  length=strlen(str);
  
        printf("The number of letters in the name is:%d",length);
  return 0;
}
