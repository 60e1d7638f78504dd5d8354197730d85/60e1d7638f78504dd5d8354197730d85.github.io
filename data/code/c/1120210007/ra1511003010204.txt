#include <stdio.h>
int main()
{
int r,s;
  scanf("%d %d",&r,&s);
    float c1=3.14*r*r;
    float s1=s*s;
  if(c1>s1)
    printf("I prefer centre 1");
  else
    printf("I prefer centre 2");
	return 0;
}