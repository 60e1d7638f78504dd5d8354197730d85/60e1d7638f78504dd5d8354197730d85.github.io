#include <stdio.h>
int main()
{
  int n;
  scanf("%d",&n);
  int i,a[100];
  for(i=0;i<n;i++)
    scanf("%d",&a[i]);
  int max=0;
  for(i=0;i<n;i++)
  {
    if(max<a[i])
    {max=a[i];
    }}
  printf("%d",max);
  

	return 0;
}