#include <iostream>
#include<string>
using namespace std;
int main()
{
std::string name,dept;
  std::getline(std::cin,name);
  std::getline(std::cin,dept);
    std::cout<<"The name is "<<name<<endl;
    std::cout<<"The department is "<<dept;
 

	return 0;
}