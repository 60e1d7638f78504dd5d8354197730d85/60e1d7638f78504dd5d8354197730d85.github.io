#include <iostream>
using namespace std;
int main()
{
    char name[10],dept[5];
    cin.getline(name,10);
    cin.getline(dept,5);
    cout<<"The name is "<<name<<"\n";
    cout<<"The department is "<<dept;
	return 0;
}