#include <iostream>
using namespace std;
int main()
{
  int a,b,c;
cin>>a>>b>>c;
  
  if(a>b && a>c)
    cout<<"The greatest number is "<<a;
  else if(a<b && b>c)
    cout<<"The greatest number is "<<b;
  else if(a<c && b<c)
    cout<<"The greatest number is "<<c;
	return 0;
}