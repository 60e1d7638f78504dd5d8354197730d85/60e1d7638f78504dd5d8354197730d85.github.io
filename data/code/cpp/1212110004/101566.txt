#include <iostream>
using namespace std;
int main()
{
 int number,number1;
 cin>>number>>number1;
 if(number>0)
   cout<<"positive";
 else if(number<0) 
   cout<<"negative";
 else
   cout<<"zero";
  cout<<endl;
   if(number1>0)
   cout<<"positive";
 else if(number1<0) 
   cout<<"negative";
 else
   cout<<"zero";
 return 0;
}