#include <iostream>
using namespace std;
int main()
{
float a,b;
  cin>>a>>b;
  if(a <0)
    cout<<"negative\n";
  else if(a>0)
    cout<<"positive\n";
  if(b<0)
    cout<<"negative\n";
    else if(b>0)
      cout<<"positive";
	return 0;
}