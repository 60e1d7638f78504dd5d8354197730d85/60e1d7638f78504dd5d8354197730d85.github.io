#include <iostream>
using namespace std;
int main()
{
  int a,b;
  cin>>a;
  cin>>b;
  if(a>=0)
    cout<<"positive\n";
  else
    cout<<"negative\n";
  if(b>=0)
     cout<<"positive";
  else
    cout<<"negative";

	return 0;
}