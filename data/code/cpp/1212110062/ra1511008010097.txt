#include <iostream>
#include <string>
using namespace std;
int main()
{	string s;
 int l,count=0;
 	getline(cin,s);
 	l=s.length();
 	//for(int i=0;i<l;i++){
     // count++;}
 	cout<<"The length of string is: "<<l;
	return 0;
}