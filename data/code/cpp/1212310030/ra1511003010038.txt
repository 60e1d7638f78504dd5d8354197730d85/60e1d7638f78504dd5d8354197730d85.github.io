#include <bits/stdc++.h>
#define ll  long long int
#define lli unsigned long long int
#define arr(a,n) lli a[n]; for(i=0;i<n;i++){ cin>>a[i]; }
#define arr_max(a,n) *max_element(a,a+n);
#define arr_min(a,n) *min_element(a,a+n);
#define pb push_back
#define mp make_pair
#define ff first
#define ss second
#define mod 1000000007
using namespace std;
 
int main()
{
int t ;
cin >>t;
while (t--)
{ int n ;
cin >> n;
 
 
 
 cout << (n/2)+1 <<endl;
 
}
 
    return 0;
}