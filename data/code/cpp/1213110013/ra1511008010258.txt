#include <iostream>
#include<string.h>
using namespace std;
int main()
{
char arr[100];
  for(int i=0;i<=strlen(arr);i++)
  {
    cin>>arr;
  }
  for(int i=0;i<=strlen(arr);i++)
  {
  cout<<arr[i]<<" ";
  }
    return 0;
}