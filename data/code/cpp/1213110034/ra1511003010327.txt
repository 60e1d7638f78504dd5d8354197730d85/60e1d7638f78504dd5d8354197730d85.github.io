#include <iostream>
using namespace std;
int main()
{
    string s;
  int i;
    cin>>s;
    for(i=0;i<s.length();i++)
    {
      if(s[i]>=65&&s[i]<=90)
      {
        s[i]=s[i]+32;
      }
    }
  cout<<s;
	return 0;
}