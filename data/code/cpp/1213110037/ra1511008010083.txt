#include <iostream>
using namespace std;
int main()
{
  char ch;
  cin>>ch;
	if(ch == 'A' || ch == 'a' || ch =='E' || ch =='e' || ch == 'i' || ch == 'I' || ch == 'O' || ch== 'o' || ch == 'U' || ch == 'u')
      cout<<ch<<" is a vowel";
  else
    cout<<ch<<" is a consonant";
	return 0;
}