#include <iostream>
using namespace std;
int main()
{
string a;
  getline(cin,a);
  for(int i = 0 ;i<a.length(); i++)
  {
    if(a[i]!=' ')
    cout<<(char)((int)a[i]-32);
    else
      cout<<a[i];
  }
	return 0;
}