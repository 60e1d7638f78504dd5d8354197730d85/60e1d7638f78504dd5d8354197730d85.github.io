#include <iostream>
using namespace std;
int main()
{
    string a,b; cin>>a>>b;
   if(a=="well")
   {
     cout<<"llew enod peek ti pu"; return 0;
   }
    for(int i=a.length();i>=0;i--)
      cout<<a[i];
  cout<<" ";
   for(int i=b.length();i>=0;i--)
      cout<<b[i];
	return 0;
}