#include <iostream>
using namespace std;
int main()
{
    int j = 31, i, count = 0;
    unsigned int num;
    int b[32] = {0};
    scanf("%d", &num);
    while (num != 0)
    {
        if (num & 1 == 1)
        {
            break;
        }
        else
        {
            count++;
            num = num >> 1;
 
        }
    }
  if(count==0)
  {
    cout<<"good number";
  }
  else
  {
    printf("\n%d", count);
  }
    return 0;
}