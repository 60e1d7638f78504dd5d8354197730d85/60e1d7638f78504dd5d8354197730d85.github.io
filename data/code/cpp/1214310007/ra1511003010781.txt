#include <iostream>
using namespace std;
int main()
{
  int p,q,r,s;
  cin>>p>>q>>r>>s;
  if(q<r||r<0||s<0)
    cout<<"Invalid Input";
  else
  {
    if(p%2==0 && q>r && s>p && (r+s)>(p+q))
      cout<<"Correct values";
    else
      cout<<"Wrong values";
  }

	return 0;
}