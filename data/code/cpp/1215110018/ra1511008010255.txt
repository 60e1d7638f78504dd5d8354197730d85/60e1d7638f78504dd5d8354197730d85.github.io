#include <iostream>
using namespace std;
int main()
{
	float r;
  	int s,a,b;
  cin >> r >> s >> a >> b;
  float f = 3.14 * r * r;
  	cout << "Area of circle is " << f << endl;
  	cout << "Area of square is " << s*s << endl;
  	cout << "Area of rectangle is " << a*b << endl;
  	
	return 0;
}