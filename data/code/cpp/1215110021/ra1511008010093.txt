#include <iostream>
using namespace std;
int main()
{
  int a,b;
  double c,d;
cin>>a;
  cin>>b;
  cin>>c;
  cin>>d;
  if(a>b)
    cout<<"The greatest no is "<<a<<endl;
  else if(b>a)
    cout<<"The greatest no is "<<b<<endl;

  if(c>d)
    cout<<"The greatest no is "<<c<<endl;
  else if(d>c)
    cout<<"The greatest no is "<<d<<endl;
	return 0;
}