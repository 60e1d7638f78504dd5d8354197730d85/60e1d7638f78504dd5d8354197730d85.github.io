#include <iostream>
using namespace std;
int main()
{
    int a,b,c,d;
    cin >>a >> b >> c >> d;
    cout<<"The sum of square is "<<a*a+b*b;
   cout<<"\nThe sum of square is "<<a*a+b*b+c*c;
   cout<<"\nThe sum of square is "<<a*a+b*b+c*c+d*d;
	return 0;
}