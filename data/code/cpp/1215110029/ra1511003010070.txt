#include <iostream>
using namespace std;
int main()
{
    int a,b,c,d,e,f,g;
  cin>>a>>b>>c>>d;
    e=(a*a*a+b*b*b);
   f=(a*a*a+b*b*b+c*c*c);
   g=(a*a*a+b*b*b+c*c*c+d*d*d);
  cout<<"The sum of cube is "<<e<<endl;
  cout<<"The sum of cube is "<<f<<endl;
  cout<<"The sum of cube is "<<g<<endl;
	return 0;
}