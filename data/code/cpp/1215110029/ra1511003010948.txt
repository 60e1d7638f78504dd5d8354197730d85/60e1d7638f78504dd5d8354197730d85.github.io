#include <iostream>
using namespace std;
int main()
{
int i,j,k,l;
  cin>>i>>j>>k>>l;
  cout<<"The sum of cube is "<<((i*i*i)+(j*j*j));
   cout<<"\nThe sum of cube is "<<((i*i*i)+(j*j*j)+(k*k*k));
   cout<<"\nThe sum of cube is "<<((i*i*i)+(j*j*j)+(k*k*k)+(l*l*l));
	return 0;
}