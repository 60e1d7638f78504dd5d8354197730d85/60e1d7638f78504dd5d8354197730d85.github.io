#include <iostream>
using namespace std;
int main()
{
float q,w,e,r,t,y;
  cin>>q>>w>>e>>r>>t>>y;
  cout<<"Area of square is="<<(q*q);
  cout<<"\nArea of rectangle is="<<(w*e);
  cout<<"\nArea of circle is="<<(3.14*r*r);
  cout<<"\nArea of triangle is="<<((t*y)/2);
	return 0;
}