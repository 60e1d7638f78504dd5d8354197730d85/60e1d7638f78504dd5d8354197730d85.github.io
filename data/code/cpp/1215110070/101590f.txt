#include <iostream>
using namespace std;
int main()
{
   int x, y, z,mul(int,int); 
      cin>>x>>y; 
      z = mul (x, y); 
      cout<<z; 
  	return 0;
     
 } 
      int mul (int p, int q) 
       { 
           return (p * q); 
       } 

