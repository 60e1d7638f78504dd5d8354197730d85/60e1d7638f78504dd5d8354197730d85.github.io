#include <iostream>
using namespace std;
int main()
{
int n,count=0,temp=0,i;
  cin>>n;
  int a[n];
  for(i=0;i<n;i++)cin>>a[i];
  for(i=n-1;i>=0;i--)
  {
    if(a[i])
      temp++;
    else
      count=count+temp;
  }
  cout<<count;
	return 0;
}