#include <iostream>
using namespace std;
int main()
{
int a[10],i,j,n,b;
  cin>>n;
  for(i=0;i<n;i++)
  {
    cin>>a[i];
  }
  for(i = 1;i < n; ++i)
    {
           if(a[0] < a[i])
           a[0] = a[i];
    }
    cout << "Largest element="<<a[0];
	return 0;
}