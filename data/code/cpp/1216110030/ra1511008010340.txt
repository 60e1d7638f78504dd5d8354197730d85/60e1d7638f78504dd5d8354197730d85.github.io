#include <iostream>
using namespace std;
int main()
{
int n,x=0;
  cin>>n;
  int a[n];
  for(int i=0;i<n;i++)
  {
    cin>>a[i];
    if(a[i]>=x)
      x=a[i];
  }
  cout<<"Largest element="<<x;
	return 0;
}