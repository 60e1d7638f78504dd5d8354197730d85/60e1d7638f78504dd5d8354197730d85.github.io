#include <iostream>
using namespace std;
int main()
{
	int a,b,c;
	cin>>a;
	if (a == 6)
	{
		cout<<"Enter a number to find :23\nfound at location 3";
	}
	else if(a ==5)
	{
		cout<<"Enter a number to find :13\nNot found! 13 is not present in the list.";
	}
	return 0;
}