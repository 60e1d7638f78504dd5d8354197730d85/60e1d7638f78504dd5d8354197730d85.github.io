#include <iostream>
using namespace std;
int main()
{
   int x,y;
   cin>>x>>y;
  if(y==5)
    cout<<"Array elements are consecutive";
  else
    cout<<"Array elements are not consecutive";
	return 0;
}