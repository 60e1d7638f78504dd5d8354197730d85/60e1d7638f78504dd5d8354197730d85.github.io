#include <iostream>
#include <algorithm>
using namespace std;
int main()
{
  int d;
  cin>>d;
  cout <<"Octal Equivalent of "<<d<<" is "<<oct<<d;
  /*
  string s = "";
  	while(d > 0) {
     	int temp = d%8;
      	s += '0' + temp;
      	d /= 8;
    }
  reverse(s.begin(),s.end());
  */
    

	return 0;
}