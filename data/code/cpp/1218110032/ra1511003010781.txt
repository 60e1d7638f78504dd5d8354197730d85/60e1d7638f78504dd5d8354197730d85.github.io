#include <iostream>
#include <ctype.h>
using namespace std;
int main()
{
  int a,b;
  char c,d;
  float e, f;
  cin>>a>>b>>e>>f>>c>>d;
  if (a>b)
    cout <<a <<" is larger.\n";
  else
    cout <<b <<" is larger.\n";
  if (e>f)
    cout <<e <<" is larger.\n";
  else
    cout <<f <<" is larger.\n";
  if (c>d)
    cout <<c<<" has larger ASCII value."; 
  else
    cout <<d<<" has larger ASCII value.";
  
     
             
              
    
 

	return 0;
}