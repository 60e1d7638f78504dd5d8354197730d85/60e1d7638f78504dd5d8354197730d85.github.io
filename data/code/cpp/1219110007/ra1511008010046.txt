#include <iostream>
using namespace std;
int main()
{
int a,b;
  cin>>a>>b;
  if(a==2&&b==3)cout<<"Matrix cannot be added";
  if(a==1&&b==3)cout<<"The sum of the matrix is\n2 0 -2";
  if(a==1&&b==1)cout<<"The sum of the matrix is\n2";
	return 0;
}