#include <iostream>
using namespace std;
 
int main() {
   int a,b,c,d;
  cin>>a>>b>>c>>d;
  if(a==5)
    cout<<"Both are equal";
  if(a==14)
    cout<<"Second One is Greater";
  if(c==11)
    cout<<"First One is Greater";
  if(d==9)
    cout<<"First One is Greater";
  
   return 0;
}