#include <iostream>
using namespace std;
int main()
{
  float num;
  int n;
  char ch;
  cin>>num;
  if(num==ch || num==n)
  {
    cout<<"Number format exception";
  }
  else 
    cout<<"Valid Input";

	return 0;
}