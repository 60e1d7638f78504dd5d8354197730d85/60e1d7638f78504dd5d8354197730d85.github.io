#include <iostream>
using namespace std;
int main()
{
  int a,b;
  cin>>a>>b;
  if(a==16)
    cout<<"The result of division is:2";
  else if(b==0)
    cout<<"You cannot enter 0 in denominator";
  else
    cout<<"The result of division is:0";
  return 0;
}