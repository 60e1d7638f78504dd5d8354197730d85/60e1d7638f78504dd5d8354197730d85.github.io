#include <iostream>
#include <cmath>
using namespace std;
int main()
{
  int a;
  cin>>a;
  if(a<0)
    cout<<"Can not take sqrt of negative number";
  else
    cout<<"square root is :"<<(float)sqrt(a);
  return 0;
}