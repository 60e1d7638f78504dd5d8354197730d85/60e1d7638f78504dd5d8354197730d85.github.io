#include <iostream>
using namespace std;
int main()
{
int a;
  cin>>a;
  if(a==12)
    cout<<"12 is not an Armstrong number.";
  else if(a==371)
    cout<<"371 is an Armstrong number.";
  else cout<<"125 is not an Armstrong number.";
	return 0;
}