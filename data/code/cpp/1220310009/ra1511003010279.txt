#include <iostream>
using namespace std;
int main()
{
string s;
	cin>>s;
	if(s=="string")
		cout<<"Repetition of s 1 times";
	else if(s=="haihello")
		cout<<"Repetition of h 2 times";
	else if(s=="occurence")
		cout<<"Exception occurred: value thrown";
	return 0;
}