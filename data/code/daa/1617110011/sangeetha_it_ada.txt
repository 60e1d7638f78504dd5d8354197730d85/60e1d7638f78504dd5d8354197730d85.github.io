#include <cstdio>
using namespace std;
 
int main()
{
    int T;
    scanf("%d", &T);
    for (int t = 0; t < T; ++t) {
        int A, B;
        scanf("%d%d",&A,&B);
        for (int i = 0; i < B; ++i) {
            int m, n;
            scanf("%d%d", &m, &n);
            //printf("%d %d\n", m, n);
        }
        printf("%d\n", A-1);
    }
    return 0;
}