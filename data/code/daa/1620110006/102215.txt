#include<stdio.h>
int addOne(int x)
{
  int m = 1;
  scanf("%d",&x);
  while( x & m )
  {
    x = x^m;
    m <<= 1;
  }
  x = x^m;
  return x;
}
int main()
{
  int x;
  printf("%d", addOne(x));
  getchar();
  return 0;
}