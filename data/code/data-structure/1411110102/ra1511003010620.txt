#include <stdio.h>
#include<stdlib.h>
int main()
{
  int n,i;
  scanf("%d",&n);
  int* a;
  a=(int *)malloc(sizeof(int)*n);
  
  for(i=0;i<n;i++)
    scanf("%d",a+i);
  
  for(i=0;i<n;i++)
    printf("%d ",*(a+i));

	return 0;
}