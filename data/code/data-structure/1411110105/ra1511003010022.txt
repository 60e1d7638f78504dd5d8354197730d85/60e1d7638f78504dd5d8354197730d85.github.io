#include <stdio.h>
#include <stdlib.h>
int main()
{
  int n,i, *ptr,sum=0;
  scanf("%d\n",&n);
  ptr=(int *)malloc(n*sizeof(int));
  if(ptr==NULL)
  {
    exit(0);
  }
  for(i=0;i<n;++i)
  {
    scanf("%d\t",ptr+i);
    sum+=*(ptr+i);
  }
  printf("Sum=%d\n",sum);
  free(ptr);
	return 0;
}