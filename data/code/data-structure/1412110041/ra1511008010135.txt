#include <bits/stdc++.h>
using namespace std;
int main()
{
	int n,i;
  float avg,sum=0;
  cin>>n;
  float a[n];
  for(i=0;i<n;i++)
  {
    cin>>a[i];
  }
  for(i=0;i<n;i++)
  {
    sum+=a[i];
  }
  printf("%.2f",sum/n);
	return 0;
}