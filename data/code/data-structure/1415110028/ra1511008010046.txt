#include <bits/stdc++.h>
using namespace std;
int main()
{
		int x;
		cin>>x;
		int arr[x];
		for(int i=0;i<x;i++)cin>>arr[i];
		cout<<"List\n";
		for(int i=0;i<x;i++)if(i%2==0)cout<<"->"<<arr[i];
  return 0;
}