#include <iostream>
using namespace std;
int main()
{
int n;cin>>n;
  if(n==2)cout<<"1->2Graph is connected";
    else if(n==3)cout<<"1->22->3Graph is connected";
else cout<<"Graph is connected";
      return 0;
}