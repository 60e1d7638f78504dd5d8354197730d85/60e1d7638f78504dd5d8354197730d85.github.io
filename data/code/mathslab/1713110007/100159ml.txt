a=input("");
b=input("");
c=input("");
if and(lt(b,a),c<prod(2,a))
fprintf("\nCheck the Order :too few nuts");
fprintf("\nCheck the Order :too few washers");
else if and(ge(b,a),ge(c,prod(2,a)))
fprintf("Order is OK\n");
else if lt(c,prod(2,a))
fprintf("Check the Order : too few washers\n");
else
fprintf("Check the Order : too few nuts\n");
end
end
end
d=plus(times(5,a),times(3,b));
f=plus(d,times(c,1));
fprintf("Total cost %0.2f",f)