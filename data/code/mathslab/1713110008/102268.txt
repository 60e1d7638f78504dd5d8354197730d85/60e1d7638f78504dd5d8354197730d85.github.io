b=input("");
n=input("");
w=input("");
tot=plus(times(b,5),times(n,3));
tot=plus(tot,w);
if n!=b
printf("Check the Order : too few nuts\n");
else if w!=times(2,b)
printf("Check the Order : too few washers\n");
else if N!=b && w!=times(2,b)
printf("Check the order : too few nuts\n Check the Order : too few washes");
else
printf("Order is OK.\n");
end
end
end
printf("Total cost %.2f",tot)
