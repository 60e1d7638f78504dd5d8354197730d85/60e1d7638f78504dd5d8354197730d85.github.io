a=input('');
b=input('');
c=input('');

if a==b && a==c,
 disp('All are equal');
elseif a>b 
 if a>c,
  fprintf('%i is the greater number',a);
 else
  fprintf('%i is the greater number',c);
 end;
else 
 if b>c,
  fprintf('%i is the greater number',b);
 else
  fprintf('%i is the greater number',c);
 end; 
end; 