a=input('');
fprintf('Part A\n');
sa=6*(a^2);
fprintf('SA = %d\n',sa);
r= sqrt(sa/(4*pi));
fprintf('Radius = %0.2f\n',r);
fprintf('Part B\n');
v=a^3;
r=nthroot(3*v/(4*pi), 3);
fprintf('Radius = %0.2f\n',r);