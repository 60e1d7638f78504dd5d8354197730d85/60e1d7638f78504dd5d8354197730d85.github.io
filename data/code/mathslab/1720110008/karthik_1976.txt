side=input('');
SAofcube=6*(side^2);
radius1=sqrt(SAofcube/(4*pi));
vol=side^3;
radius2=((3*vol)/(4*pi))^(1/3);
fprintf('Part A\n');
fprintf('SA = %d\n', SAofcube);
fprintf('Radius = %.2f\n', radius1);
fprintf('Part B\n');
fprintf('Radius = %.2f',radius2);